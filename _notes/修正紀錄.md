# VFB 專題修正紀錄

規則見 `../../CLAUDE.md`（共通核心）與 `../../types/tool.md`（工具導覽型）。
這份檔案**不部署**：GitHub Pages 的 workflow 在上傳前會 `rm -rf _notes`。

> **讀待辦請看檔案最後那一節〈現在還沒解的〉。**
> 各節裡的「待辦更新」是**當時**的快照——後來解掉的多半在新的一節被標成已解，
> 原處那一行沒有回頭改。所以 `grep '^- \[ \]'` 抓出來的不是現況。

---

## 反覆出現的錯誤型態

到目前為止（探針階段）出現三型，每一型都不只一次：

1. **把說明文件的節錄當成資料的全部。** 文件站是人寫的導覽，只挑重點講；
   資料庫裡的東西比文件多。凡是「VFB 有 N 套／N 筆」這種句子，都要問 API，不要抄文件。
2. **把「筆數」當成「個數」。** API 回的 `count` 是**列數**。同一個東西會因為
   對位到多套 template、掛在多個類別底下而佔好幾列。
3. **靜默截斷。** API 不回錯誤，只回一個看起來正常的空陣列或短清單。
   凡是翻頁、凡是有上限的地方，都要自己驗「拿到的筆數 ＝ 它說的筆數」。

---

## 2026-09-13　探針階段

### 1. 「VFB 有四套 template」是錯的，實際上有 10 套
- **錯在哪**：`vfb_plan.docx` 第 3.1 節照文件站 `/docs/concepts/templates/` 寫了四套
  （JRC2018 腦與 VNC、JFRC2、L3 幼蟲、成蟲 VNS）。
- **事實**：`/search?query=*&filter_types=Template` 回 **10 套**。文件沒提到的六套是
  `VFB_00030786`（Ito2014 成蟲腦）、`VFB_00050000`（L1 幼蟲 ssTEM，Cardona/Janelia）、
  `VFB_00101384`（JRC_FlyEM_Hemibrain，hemibrain 自己的 template）、
  `VFB_00110000`（成蟲頭部 McKellar2020）、`VFB_00120000`（成蟲 T1 腿 Kuan2020）、
  以及 JRC2018 的 VNC 版本本來就在清單裡。
- **型態**：第 1 型。

### 2. JFRC2 與 Ito2014 是兩個不同的 template，不是同一個的兩個名字
- **錯在哪**：文件站把它們寫成「JFRC2/Ito2014」，看起來像同一套的別名，計畫書照抄。
- **事實**：`VFB_00017894`（JFRC2，58 個分區，CC-BY-NC-SA 4.0）與
  `VFB_00030786`（Ito2014，75 個分區，CC-BY-SA 4.0）是兩筆，連授權都不同。
- **型態**：第 1 型。

### 3. 知識庫裡的 Template 節點有 20 個，比搜尋看得到的多一倍
- **事實**：Cypher `MATCH (t:Template)` 回 20 個，多出來的十個是 `VFBc_` 開頭的附屬影像
  （每套 template 各一個）。兩個數字都對，答的是不同的問題。
- **本課的裁決**：頁面上講「有幾套 template」一律用**搜尋看得到的 10 套**；
  需要提到 `VFBc_` 時一定寫明那是附屬影像。（共通核心第 2 節：爭議要有裁決。）

### 4. FlyCircuit 的「16,127」是神經元數，但 REST 回的 count 是 68,698
- **錯在哪**：差點把 `run_query?...&query_type=DatasetImages` 的 `count` 當成神經元數寫進教材。
- **事實**：那 68,698 是**列數**。同一顆神經元同時對位到 JFRC2 與 JRC2018U（兩列），
  又同時掛在「adult neuron」與「expression pattern fragment」等類別底下，
  平均一顆佔 4.265 列。真正的顆數要用 Cypher 算：
  `MATCH (i:Individual)-[:has_source]->(:DataSet {short_form:'Chiang2010'}) RETURN count(DISTINCT i)`
  → **16,127**，跟文件站寫的一致。
- **型態**：第 2 型。

### 5. REST 的翻頁到 offset 50,000 就停，而且不報錯
- **錯在哪**：探針第一版把「翻完的列」拿去算不重複 id，得到 11,723，
  差一點就寫成「FlyCircuit 有 11,723 顆」。
- **事實**：`offset=45000` 還回得到 1,000 列，`offset=50000` 開始回**空陣列**，
  沒有任何錯誤訊息。實際翻得到 50,000 列，而 `count` 說有 68,698。
- **怎麼修**：`paged()` 現在回傳 `truncated` 旗標，資料檔裡分成
  `rest_reported_count`／`rest_rows_fetched`／`rest_truncated` 三個欄位，
  顆數改用 Cypher 算。
- **型態**：第 3 型。**這一條要寫進教材**——它是「為什麼要學 API 那一層」最好的例子。

### 6. 連線資料集的版本跟文件寫的不一樣，而且新舊版同時在庫裡
- **事實**：`/list_connectome_datasets` 現在回 8 套，BANC 是 **v888**（文件寫 v626）。
  Cypher 算每套的神經元數時，**兩版都在**：`Bates2025` = BANC v626（80,003 顆）、
  `Bates2026` = BANC v888（64,546 顆）；hemibrain 也是 `Xu2020Neurons` v1.0.1 與
  `Xu2020NeuronsV1point2point1` v1.2.1 兩列；male CNS 是 `Berg2025` v0.9 與 `Berg2025a` v1.0。
- **意義**：這不只是「不能把重疊組織的資料集相加」，**連同一套資料的新舊版也不能相加**。
  PART 4 要拿這張表當例子。
- **型態**：第 1 型。

### 7. 文件提到的 FANC 不在連線資料集清單裡
- **事實**：`/docs/data/em/` 列了 FANC（CATMAID，成蟲雌性 VNS），
  但 `/list_connectome_datasets` 只有 `catmaid_fafb` 與 `catmaid_l1em`。
- **還沒查清楚**：是被移除了，還是它不算「connectome dataset」而只有骨架。
  **寫 PART 4 之前要查**，不要在頁面上出現「VFB 有 FANC」這種沒驗過的句子。

### 8. 授權是逐筆的，而且有一套是 NC（禁止商業使用）
- **事實**（都從 `get_term_info` 的 `Licenses` 欄讀出來）：
  JRC2018 系列（腦、VNC）與 JFRC2 是 **CC-BY-NC-SA 4.0**；
  Ito2014、Wood2018、Court2018、McKellar2020 是 CC-BY-SA 4.0；
  hemibrain template 與 Kuan2020 是 CC-BY 4.0；
  FlyCircuit 是自訂的 **FlyCircuit License**（不是任何一種 CC）。
- **意義**：`types/tool.md` 那條「逐筆查授權」在第一次抓資料就驗證了。
  本課是非營利教學，NC 沒有問題，但**圖說要寫出是哪一種**，
  而且 FlyCircuit 那套要照 VFB 的要求引 Chiang 2010。

---

## 探針本身的兩個設計決定（寫下來以免日後又改回去）

- **資料檔裡不放時間戳。** 抓取時間、耗時、雜湊全部放 `out/_manifest.json`。
  否則 VFB 換版後重跑，`diff` 會把每一個檔案都標成「改了」，真正的變動反而看不見。
- **`/status` 只留 health 與 upstream，丟掉快取與連線計數。**
  那些數字每秒都在動，留著會讓每一次重跑都報「service 內容變了」，
  把第一項的變動偵測變成雜訊。（第一版就是這樣，跑兩次才發現。）

---

## 待辦

- [ ] 查清楚 FANC 的狀況（第 7 條）。
- [ ] `ListAllAvailableImages` 對 medulla 只回 4 筆，但 `ImagesNeurons` 回 226,524 筆——
      這兩個查詢的差別要弄懂再寫 PART 3。
- [ ] `VFB_00050000`（L1 幼蟲 ssTEM）的 `Domains` 是 0，要確認是真的沒有畫分區，
      還是分區掛在別的地方。（PART 1 的表照實寫 0。）
- [ ] PART 1 的作業單第 2 步寫「一共 46 列」，是拿 API 的 count 寫的；
      要在網站介面上實際點一次，確認畫面上顯示的筆數也是 46。

---

## 2026-09-13　PART 1

### 9. 「分區數＝清單筆數減一」這條規則，十套裡有兩套是錯的
- **錯在哪**：`get_term_info` 的 `Domains` 欄第一格（index 0）通常是 template 自己，
  所以第一版探針寫成 `len(Domains) - 1`。
- **事實**：`VFB_00101384`（hemibrain template）與 `VFB_00120000`（成蟲前足）
  **根本沒有那一格**，減一就少算一個。正確的數字是 97 與 4，不是 96 與 3。
- **怎麼抓到的**：同一個數字用兩種算法各算一次——一種數清單、一種直接跑
  站上的 `PaintedDomains` 查詢——然後比對。兩個算法一致才寫進教材。
  **不是眼睛看出來的，是程式比出來的。**
- **教訓**：「位置」不能拿來當身分。要排除某一筆，就比對它的 id，不要假設它排在第一個。
- **型態**：第 3 型的變形——資料結構不一致，而且不會報錯。

### 10. 頁面上的英文引文，有一條是改寫過的
- **錯在哪**：頁面上寫成
  「The only way to put a FlyWire neuron next to a split-GAL4 expression pattern is to bridge between them.」
  這句讀起來完全通順，也確實在說明文件講的意思範圍內。
- **事實**：原文是一個更長的句子，前面還有
  「Data on VFB arrives registered to whichever standard its producers used — JRC2018 unisex, ... —
  and the only way to put a FlyWire neuron ...」。
  頁面上那個大寫的 The 是摘要過程加上去的，原文沒有這個句子。
- **另外三條**是相反的問題：引到一半就補上句點，而原文還沒結束
  （`is the standard.` 原文是 `is the standard — which looks broadly the same...`；
  `you need EM.` 原文是 `you need EM (connectivity data).`）。
- **怎麼抓到的**：新寫的 `scripts/quote_audit.py`，把每一條 `<p lang="en">`
  逐字對回說明文件的純文字。**四條全部是程式抓到的，人看不出來。**
- **稽核程式本身踩了兩個坑**（跟共通規範第 3 節列的是同一類）：
  1. 剝標籤之後句末的句點會變成「template .」，不先把標點前的空白去掉，
     四條真引文會全部被誤判成對不到。
  2. 引文中間用 `…` 標省略，但正規化用的 NFKC 會把 `…` 轉成三個句點，
     所以**要先按 `…` 切段，再正規化**。
- **型態**：新的一型——**改寫過的引文讀起來比原文通順**，所以特別難發現。

### 11. 側視圖不先把左半腦拿掉，等於只畫了一片視葉
- **錯在哪**：第一版的側視圖從左側看整顆腦，結果最靠近攝影機的左視葉把其他全部擋住，
  34 個分區裡只看得見 30 個，而且都是同一片。
- **怎麼修**：先裁掉左半腦（中線由兩片視葉質心的中點定），再從中線往外看。
- **型態**：共通規範第 7 節那條「攝影機站在軸的哪一端決定哪一面擋住哪一面」的實例。

---

## 寫過但被拿掉的

- **計畫書原本寫「VFB 有四套 template」**（照文件站抄的），PART 1 改成十套並列出全部。
  不要再改回四套。
- **側視圖原本畫整顆腦**，見第 11 條。
- **殘差圖原本只報全體中位數 5.74 µm**。後來補上「解參數用的 412 顆 5.89、留出的 103 顆 4.73」，
  因為只報一個數字，讀者無從判斷這個轉換有沒有過度貼合。
  D03 的 json 裡記的是留出那組（4.73），跟全體那個數字不一樣是正常的——
  **兩個數字量的不是同一件事**，頁面上兩個都寫。

### 12. REST 回的授權欄位是不穩定的，同一個 id 問五次會給兩種答案
- **錯在哪**：PART 1 那張 template 表的授權欄，第一版照 `get_term_info` 的
  `Licenses` 欄抄。十套裡有四套抄錯（Ito2014、Wood2018、Court2018、McKellar2020
  都被寫成 CC-BY-SA 4.0，實際是 CC-BY 4.0），`VFB_00050000` 還被寫成兩種授權。
- **事實**：同一個 id 連問五次，`VFB_00110000` 回過三次 `CC-BY_4.0`、
  兩次 `CC-BY-SA_4.0`。**不是我抄錯，是那一欄本身會變。**
- **怎麼判定哪個對**：改問知識庫——
  `MATCH (t:Template)-[*1..2]-(l:License) RETURN t.short_form, collect(DISTINCT l.label)`
  每一套 template 都只連到**一個** License 節點，連問三次結果都一樣。
  授權實際掛在 template 影像來源的那個 dataset 上（`has_source` → `has_license`）。
- **怎麼修**：探針的授權改從知識庫取，REST 那一欄照樣存下來當對照，
  兩邊不同就在 note 裡列出來。頁面上加一個方框告訴讀者「看到不一樣以表為準」，
  並指出最終依據是原始論文的授權聲明。
- **型態**：第 3 型（靜默不一致）的極端版——**連重跑同一個查詢都會得到不同答案**。
  這條也是 `types/tool.md` 那句「授權是逐筆掛在資料上的」的下一層：
  逐筆還不夠，還要問對地方。
- **順帶的好消息**：釐清之後，答案反而更好記——
  **只有 JRC2018 那一家（腦、VNC）與 JFRC2 帶 NC，其餘七套都是 CC-BY 4.0。**

### 13. 兩個交叉檢查都是「多做一次」換來的
第 9 條與第 12 條有同一個形狀：**同一個數字用兩條路各取一次，然後比對。**
分區數（數清單 vs 跑查詢）抓到兩套算錯；授權（REST vs 知識庫）抓到四套抄錯。
兩次都不是看出來的，是比出來的。**這件事要變成這個專題的預設做法**——
凡是要寫進教材的數字，先問「有沒有第二條路可以問到同一個數字」。

---

## 2026-09-14　PART 1 回頭修

### 14. 授權方框「以這張表為準」跟「最終依據是原始論文」互相打架
- **錯在哪**：第 3 節那個方框原本寫「如果你在網站上看到的授權跟這張表不一樣，**以這張表為準**」，
  結尾又寫「真要用在正式場合的話，回頭查那套 template 原始論文的授權聲明，那才是最終依據」。
  使用者指出：**如果最終都要回頭查，那憑什麼叫讀者以這張表為準。**
- **問題的形狀**：一個方框同時回答了兩個不同的問題，而兩個答案的層級不同——
  1. 「VFB 的兩個介面給我不同答案，該信哪一個？」→ 知識庫（我們有證據：面板重問會變、知識庫重問一致）。
  2. 「我能不能拿這套 template 來用？」→ 原始釋出者。VFB 兩邊都只是**轉錄**。
  第 12 條查清楚的是第一個問題，寫的時候卻用「為準」這個帶裁決意味的詞把它講成了第二個問題的答案。
- **怎麼修**：把方框拆成兩段。第一段只說「這張表記的是知識庫那一個，看到兩邊不一樣不是你看錯」；
  第二段明講「這張表回答的是 VFB 記了什麼，不是你可以做什麼」，並把表的定位寫成
  **索引（該去查哪一套、大概會查到什麼），不是依據**。第 143 行「這一欄要先看」一併改掉，
  免得它又把讀者留在那一欄。
- **教訓**：**「以 X 為準」這句話要先問「為準是為了回答哪個問題」。**
  我們對某個欄位做了交叉驗證，得到的是「這兩個來源哪個可信」，不是「這個數字有權威」。
  往後凡是要寫「以…為準」，先確認裁決的範圍沒有超出證據的範圍。
- **型態**：新的一型——**證據的範圍與句子的範圍對不上**。跟前面十三條不同，
  這一條不是資料錯，四道稽核也抓不到（數字全對、引文全對、標籤全平衡）。
  是使用者讀出來的。

### 待辦（由第 14 條衍生）
- [ ] 寫 PART 2–6 時，凡是出現「本課以 X 為準」「一律指 X」這類裁決句，
      逐句檢查裁決的範圍有沒有超出手上證據的範圍。目前站上還有兩句同形的
      （第 3 節「本課往後講『有幾套 template』一律指這十套」、
      「本課往後凡是沒有特別說明的成蟲腦指的都是 JRC2018Unisex」）——
      這兩句是**本課內部的用語約定**，範圍沒有外溢，維持原狀。

### 15. 作業單第 1 步照著做會卡住：搜尋結果裡根本沒有那個編號
- **錯在哪**：第 7 節作業單原本寫「在搜尋框輸入 `JRC2018Unisex`，點進結果。
  資訊面板（Term Info）裡會出現它的編號 VFB_00101567」。
  使用者實際去操作，回報「輸入完之後就會出現很多項，但就是沒有 VFB_00101567」。
- **實測結果**（`scripts/vfb_probe.py` 新增的 `worksheet` 探針，2026-09-14）：
  這一句話裡有**三個各自獨立的坑**，任何一個都足以讓學員卡死。

  1. **編號不在結果清單裡。** 搜尋回的每一列只有名字，編號要點進去之後才在
     Term Info 與網址列出現。原句「輸入…點進結果。資訊面板裡會出現編號」
     語法上沒錯，但讀起來像是「輸入完就看得到編號」——
     學員因此在清單裡找編號，永遠找不到，也就走不到「點進去」那一步。
  2. **回來的 103 筆裡有 99 筆不是 template。** 名字全長成
     「bulb on JRC2018Unisex adult brain」「medulla on JRC2018Unisex adult brain」。
     原因很單純：畫在一套 template 上的每一塊，名字裡都帶著那套 template 的名字，
     所以搜尋 template 的名字會把它的整份分區地圖一起撈回來（JFRC2 是 129 筆裡 127 筆）。
     **這裡我自己先錯了一次**：第一版探針照「型別是不是 Synaptic_neuropil」分類，得到 97，
     就把 97 寫進頁面說「97 筆的名字長成某某 on …」。但顎神經節的型別是 `Ganglion`
     而不是 `Synaptic_neuropil`，名字卻一樣帶「on …」——**照型別分出來的數字，
     拿去描述學員在畫面上看到的名字，本來就對不上**。改成照名字分（99），
     並加一條 `all_rows_accounted_for` 檢查：分類完的各群要加得回總數，加不回就是還有沒想到的東西。
  2b. **搜尋 JRC2018Unisex 會連另一套 template 一起撈回來。** 剩下的 4 筆不是 2 筆：
     2 筆是 VFB_00101567（你要的），另外 2 筆是 **VFB_00200000 JRC2018Unisex<u>VNC</u>**
     ——腹神經索那一套。名字只差三個字母，學員很可能點錯。原本的作業單完全沒提這件事。
  3. **學員打的那個字串不是這個詞的正式名字。** `get_term_info` 的 `Name` 是
     **JRC2018U**，`JRC2018Unisex` 只是同義詞。所以目標在結果裡出現**兩列**
     （rank 0 的 `JRC2018Unisex (VFB_00101567)` 與 rank 54 的 `JRC2018U (JRC2018Unisex)`），
     而畫面上可能顯示成 JRC2018U——跟學員要找的字串長得不一樣。
     （JFRC2 沒有這個問題，它的正式名字就是 JFRC2。）
- **怎麼修**：
  - 作業單開頭先加一個方框，明講「第一步就會卡住，而且卡在哪」：
    會拿到一百多筆、**清單裡不會出現編號**、要照名字認。
  - 第 1 步改成「搜尋，然後看懂你拿到的是什麼」，而且**把 103 筆逐群交代完**
    （99 筆名字帶「on …」＋ 2 筆 JRC2018UnisexVNC ＋ 2 筆才是目標），
    講明正式名字是 JRC2018U，並給**直達網址**
    `https://virtualflybrain.org/reports/VFB_00101567` 當保險
    （這個網址不是我們編的——VFB 自己的 PaintedDomains 查詢回的每一列就是連到這種網址，
    實測回 200）。
  - 「確認你點對了」獨立成第 2 步，把「看到編號」從第 1 步拆出來。
  - 第 4 步（原第 4 步「隨便點開一顆神經元」）改成指定的一顆
    Cha-F-100205（VFB_00005010）＋直達網址，並寫出答案是兩套
    （VFB_00017894 與 VFB_00101567）。「隨便點開一顆」是不可重現的指示——
    學員點到別的，可能只有一套，然後以為自己做錯了。
  - 末尾補一個方框，把這個坑翻成教學點：**搜尋一個 template 的名字，
    回來的九成不是 template，而是它的分區**——這正是第 2 節「template 是兩件事」
    的直接後果。認名字的訣竅只有一條：**看有沒有「on … 」那一段**。
- **順帶驗掉的兩條舊待辦**：
  - PaintedDomains 對 VFB_00101567 實跑 `count=46`，查詢標籤實際就叫
    `Painted domains for JRC2018U`，跟頁面寫的一致（原待辦是「46 列還沒在介面上點過」，
    現在是用 API 驗的，**網站畫面上的排序與欄位仍未親眼看過**，這一條沒有完全消掉）。
  - JFRC2 的 PaintedDomains 實跑 `count=58`，與第 3 節的表一致。
- **型態**：新的一型——**指令在 API 層面成立，在學員的螢幕前不成立**。
  這一條跟第 14 條一樣，四道稽核全部不會響（數字全對、引文全對、標籤全平衡），
  但它比第 14 條嚴重：第 14 條是讀者讀完覺得邏輯怪，這一條是**讀者當場做不下去**。
- **做法上的改變**：新增 `worksheet` 探針，把作業單的每一步照學員的做法跑一次，
  輸出 `out/worksheet.json`。**作業單的數字從此跟正文的數字同一個標準**——
  以前硬條件只蓋到正文，作業單是憑印象寫的，這就是代價。

### 待辦（由第 15 條衍生）
- [ ] **每一份作業單在發布前都要有人照著做一次**，而且要從學員的起點做
      （空白瀏覽器、不知道編號、不知道正式名字）。API 驗得過不等於做得下去。
- [ ] 網站畫面上的搜尋結果**排序與顯示欄位**仍未親眼確認過（本次全部用 API 驗）。
      第 1 步的「它排在最前面」這種話因此沒有寫進頁面——頁面只寫「照名字認」＋直達網址。
      日後若要寫任何跟畫面排序有關的句子，要先實際開瀏覽器看。
- [ ] PART 2–6 的作業單一律照第 15 條的形狀寫：
      **指定的例子（不要「隨便挑一個」）＋ 預期看到的數字 ＋ 直達網址 ＋ 會卡在哪的預告。**

### 16. 第 15 條的修法是錯的：我拿 API 的筆數去描述學員的螢幕
- **錯在哪**：第 15 條把作業單改寫成「回來的 103 筆裡，99 筆是分區、2 筆是 VNC、
  2 筆才是目標」，並說「清單裡不會出現編號」。使用者實際操作回報：
  **「我只有看到 8 筆，每一筆的名字都長成『某某 on JRC2018Unisex adult brain』，
  裡面有各自的編號，但就是沒有 VFB_00101567。」**
  ——筆數不對（8 vs 103）、「清單裡沒有編號」也不對（清單裡有編號）。
- **查出來的事實**：
  1. **網站的搜尋框打的根本不是我一直在查的那支 API。** 探針用的是
     `v3-cached.virtualflybrain.org/search`；網站主搜尋框打的是
     **`solr.virtualflybrain.org/solr/ontology/select`**。兩支不同的服務。
  2. **文件站寫的 SOLR 參數，跟前端實際送的也不一樣。** `/docs/apis/solr/` 給的
     `bq`／`pf`／`fq` 與 `v2.virtualflybrain.org` 的 `main.bundle.js` 裡那組對不上
     （前端多了 `pf=label^250 synonym^120`、`facets_annotation:Class^200.0`，
     `short_form:VFB*` 的權重也從 ^100 變成 ^50）。**又是第 1 型**：
     文件是人寫的導覽，不是實作。
  3. **前端還會把每一筆再拆開。** `explode_fields`／`explode_arrays` 把一筆結果
     拆成「名字一列、`編號 (名字)` 一列、每個同義詞各一列」——
     使用者說的「每一筆裡面有各自的編號」就是這個。
  4. 所以同一個字串有**三個**都不一樣的數字：REST 103 列、SOLR 47 筆、
     explode 後 148 列。**沒有一個是 8。**
- **我沒查出來的**：為什麼是 8、以及為什麼目標不在最前面。照前端那組參數重跑，
  VFB_00101567 應該佔掉 explode 後的第 0、1、2 列（第 1 列字面上就是
  `VFB_00101567 (JRC2018Unisex)`）。**我的重建跟使用者的螢幕不一致，
  而我沒有辦法從這裡看到那個螢幕。**
- **根本問題**：第 15 條我已經寫了「網站畫面上的排序與顯示欄位沒有親眼看過」，
  **然後照樣把 API 的筆數寫成學員會看到的筆數**。警告寫在 figsrc 裡，
  正文卻照寫不誤——等於自己給自己開了一張免責聲明，然後繼續犯同一個錯。
  這比第 15 條嚴重：第 15 條是沒想到，這一條是想到了還做。
- **怎麼修**：作業單改成**網址優先**。
  - 五步全部給 `reports/編號` 網址（實測是 302 轉到
    `v2.virtualflybrain.org/org.geppetto.frontend/geppetto?id=…`，最終 200）。
  - **頁面上不再出現任何「你會看到幾筆」。**
  - 搜尋框降級成第 5 步的**選做**，而且只講「絕大多數不是 template」這種
    不依賴筆數的話，外加一句「找不到不是你的問題，用網址就好」。
  - 末尾加一個方框把這件事翻成教學點：**「幾筆」是介面的性質，不是資料的性質**
    ——這正是第 5 節「筆數不是個數」換個地方又出現一次。三個數字（103／47／148）
    寫進去當證據，但明講它們都不是「你應該看到 N 筆」。
- **探針**：`worksheet` 加一支 `solr_site_search()`，照**前端 bundle 裡抄出來的參數**
  問 SOLR，並記 `solr_num_found`、`solr_exploded_rows`、`solr_target_first_ranks`、
  `solr_top8_as_displayed`。參數來源寫在函式的 docstring 裡——那不是文件抄的。
- **型態**：第 15 條那一型（指令在 API 層面成立、在螢幕前不成立）的**加重版**，
  再加一條新的：**我把「我查得到的東西」當成「使用者會遇到的東西」**。
  探針查的是資料，學員用的是介面，兩者中間隔著一支我沒查過的 API 跟一層前端邏輯。

### 待辦（第 16 條，這幾條沒解掉之前不要再寫任何介面描述）
- [x] ~~為什麼使用者只看到 8 筆、而且目標不在其中~~ ——**第 18 條解掉了**：
      那是說明文件站的搜尋框，程式碼裡寫死 `Z=8`（只顯示 8 筆），
      而且排序對打字的大小寫敏感，目標因此掉出前 40 名。
- [ ] PART 2 要講搜尋，**在解掉上一條之前不要動筆**。PART 2 的主題就是搜尋，
      拿現在這個理解去寫一定會再錯一次。
- [ ] 本課至今所有關於「網站畫面」的句子都沒有親眼驗證過。
      寫 PART 2 之前把它們列一遍，能改成網址的改成網址，改不掉的要實際看過再留。

### 17. 追第 16 條的下落：VFB 同時有兩個線上 app，而我只拆了其中一個
使用者補充：那 8 筆是**打字時跳出來的下拉選單**（typeahead），按 Enter 會進到這 8 筆的其中一筆——
也就是說下拉選單就是唯一的入口，沒有另一頁「完整結果」。循這條線查下去：

- **前端不是只有一支搜尋。** `v2.virtualflybrain.org` 的 `main.bundle.js` 裡有**四組**
  SOLR datasource 設定，參數各不相同（rows 100/100/100/500、fq 與 bq 都不一樣）。
  其中兩組的 `fq` 要求 `facets_annotation:has_neuron_connectivity`——那是連線相關的元件用的，
  實測對 `JRC2018Unisex` 回 **0 筆**（template 沒有連線資料）。
- **結果在瀏覽器裡還會被重排與拆開。** 一個 web worker 把每一筆拆成
  「`同義詞 (正式名)`」與「`正式名 (編號)`」兩種列——**所以清單裡每一列都帶編號**，
  使用者說的「裡面有各自的編號」就是這個。第 16 條我寫「清單裡不會出現編號」是錯的。
  拆完之後再用一個**自訂 sorter** 重排（規則依序是：完全相同 → 逐詞匹配 →
  匹配位置離字首多近 → **FBbt 類別優先於 VFB 個體** → VFBexp → 編號裡的匹配位置 → 字母序）。
  排序完全不是 SOLR 給的分數。
- **而且 VFB 同時有兩個線上 app。**
  `v2.virtualflybrain.org/org.geppetto.frontend/geppetto`（舊的 geppetto 介面，
  文件站的 Launch VFB 按鈕指向它、`virtualflybrain.org/reports/<編號>` 也 302 轉到它）
  與 **`v3.virtualflybrain.org`**（另一套 React 介面，搜尋框寫著「I'm looking for...」）。
  兩邊的 bundle 我都拆了，v3 的主搜尋用的參數跟**文件站寫的那組**一致（rows 150）。
- **結論：我重現不出來。** 上面每一組設定我都照抄實跑過，
  `VFB_00101567` 都排在最前面（v2 主搜尋那組，拆開後它佔掉第 0、1、2 列，
  第 1 列字面上就是 `VFB_00101567 (JRC2018Unisex)`）。**沒有任何一組會產生
  「8 筆、全是分區、沒有 template」這個結果。**
  我能做的靜態分析到此為止——剩下只能實際開瀏覽器看。

**這一條要記住的不是技術細節，是這個：**
一句「在搜尋框輸入 X，會看到 Y」，背後至少疊了五層我事前一層都沒查：
哪一個 app（v2／v3）→ 哪一個元件的設定（四組之一）→ SOLR 的排序 →
前端的拆列 → 前端的自訂重排 → 下拉選單顯示得下幾列。
**這種句子本課以後一律不寫。** 作業單給網址，就完全不必碰這五層。

### 待辦（第 17 條）
- [x] ~~問使用者他開的是哪一個~~ ——已答：他開的是 `www`（說明文件站），兩個 app 都不是。
- [ ] 真的要寫介面的話，用瀏覽器實際看過再寫，不要再從 bundle 推。
- [ ] `reports/<編號>` 現在 302 指向 v2。**這等於把學員固定在 v2**。
      這是刻意的選擇（那是 VFB 自己維護的入口，將來他們改指向 v3，我們的連結自動跟著走），
      但要知道有這件事；如果 v3 變成預設而兩邊介面差很多，PART 1 的作業單要回頭複查。

### 18. 找到了：我叫學員去的那個網站，根本不是 VFB 的 app
使用者回：「網址就是按你在教學檔案裡給的連結。」——那就對上了。
作業單第一版的開場是「打開 <a href="https://www.virtualflybrain.org/">virtualflybrain.org</a> 跟著做」，
而 **`www.virtualflybrain.org` 是 VFB 的說明文件／介紹網站，不是 VFB 的檢視器**。
app 在 `v2.virtualflybrain.org/org.geppetto.frontend/geppetto`（`reports/<編號>` 就是 302 轉到那裡）。
**我把文件站當成 app，然後叫學員在文件站的搜尋框裡找資料庫裡的詞。**

那個框的程式碼（`www.virtualflybrain.org/js/app.*.js`）寫得很清楚：

```js
let Z = 8, q = 40;   // 跟 SOLR 要 40 筆，畫面上只顯示 8 筆
```

拿回來之後再依「**跟你打的字是否完全相同**」重排：
0 = 編號相同、1 = 正式名相同、2 = 同義詞相同、3 = 其餘（維持 SOLR 原順序）。

照這組參數逐字模擬，四種打法的結果（已寫進探針，`out/worksheet.json` 的 `docsite_search_box`）：

| 打進去的字 | 目標在前 40 名的第幾名 | 畫面上看得到嗎 |
|---|---|---|
| `JRC2018Unisex` | 第 0 名 | 看得到 |
| `JRC2018unisex` | 排不進前 40 | **看不到** |
| `JRC2018Uni`（還在打） | 排不進前 150 | **看不到** |
| `JRC2018` | 排不進前 150 | **看不到** |

**只差一個字母的大小寫，目標就從第 1 名掉到 40 名以外**，而那個框只拿前 40 筆、只顯示 8 筆，
於是畫面上就是使用者看到的那 8 筆——全部是「某某 on JRC2018Unisex adult brain」。
而且打字途中的每一個前綴都排不進去，所以整個輸入過程它從來沒出現過。
**使用者沒有做錯任何事。**

- **型態**：第 16 條那一型（我把「我查得到的」當成「使用者會遇到的」）的**源頭**。
  前面第 15、16 兩條我一直在調整「搜尋結果應該長什麼樣」，卻從來沒問過
  **學員被我送到哪一個網站**。錯的不是描述，是起點。
- **怎麼修**：
  - 作業單五步全部給 `reports/<編號>` 網址，開場不再叫人「打開 virtualflybrain.org」。
  - 第 5 步（選做）改成帶著答案去做實驗：明講**有兩個不同的搜尋框分屬不同網站**，
    並且叫學員故意把 U 打成小寫，親眼看它消失——
    **把這個坑本身變成這一步要教的東西**（40 筆／8 筆／大小寫那三個數字都寫出來）。
  - 末尾方框改寫成「為什麼一律給網址」：**「幾筆」是介面的性質，不是資料的性質**，
    並接回第 5 節的「筆數不是個數」與 PART 6 的「繞過介面直接問資料庫」。
  - 探針加 `solr_docsite_palette()`，參數與 `Z=8, q=40` 兩個常數都註明是從
    文件站的 js 抄的，四種打法的名次存進 `out/worksheet.json`。
- **這一輪三條（15、16、18）合起來的教訓**：
  **一句「在網站上怎麼做」，第一件要查的不是怎麼做，是「哪一個網站」。**
  我花了兩輪在調整第二層（結果長什麼樣），而錯在第一層。
  `types/tool.md` 要補一條：工具導覽型的教材，**每一個操作步驟都要寫明是在哪一個網域上做**，
  而且那個網域要在頁面上出現，不能只藏在連結裡。

### 待辦更新
- [x] 第 17 條那條「問使用者他開的是哪一個」——已解，答案是文件站（www），不是 app。
- [x] `types/tool.md` 補「操作步驟要寫明網域」那一條。已補成新的一節〈滑鼠那一層〉。
- [ ] PART 2 主題就是搜尋：現在知道 VFB 至少有三個行為不同的搜尋框
      （文件站命令面板、v2 檢視器、v3 檢視器），**PART 2 要先把這件事講清楚再講怎麼搜**。
- [ ] 本課所有介面描述仍然沒有用瀏覽器親眼看過。第 18 條是靠讀前端原始碼解開的，
      這次剛好讀得到；下一次不一定。

### 19. 說了「去看什麼」，沒說「去哪裡看」——同類的共六處
使用者指出第 4 步：「看它的影像列在哪些 template 底下」——該寫的是
**去看 Term Info 裡的 `Aligned to` 那一欄**。這是對的，而且照規矩把同類的全掃了一遍，
整頁一共六處，全部補上「去哪裡看」：

| 位置 | 原本 | 改成 |
|---|---|---|
| 第 1 步 | 「資訊面板（Term Info）…會出現編號」 | Term Info **分頁**的 **`Name`** 欄 |
| 第 2 步 | 「在同一個面板裡找到…查詢」 | Term Info 往下捲的 **`Queries`** 區 |
| 第 3 步 | 「做同樣的事」 | 同上，並寫出這次的查詢叫什麼 |
| 第 4 步 | 「看它的影像列在哪些 template 底下」 | Term Info 的 **`Aligned to`** 欄 |
| 授權方框 | 「如果你在網站上看到的授權…」 | Term Info 的 **`Licenses`** 欄 |
| 結尾 note | 「可以在網站上查該筆資料的來源資料集名稱」 | 同一個 `Licenses` 欄也標來源資料集 |

**掃的過程中抓到一個真的錯**（不是寫法問題，是事實錯）：
第 1 步原本寫「面板上寫的是 **JRC2018U**，不是你以為的 JRC2018Unisex」。查了才知道，
`get_term_info` 有**兩個**名字欄位：`Meta.Name`＝`[JRC2018Unisex](VFB_00101567)`（面板的 Name 欄顯示這個）、
頂層 `Name`＝`JRC2018U`（**查詢的標籤用的是這個**，所以那支查詢叫 "Painted domains for JRC2018U"）。
v3 前端的程式碼取的是 `Meta.Name`。**所以面板上寫的其實是 JRC2018Unisex，我寫反了。**
改成講清楚「同一個東西在同一個面板上有兩個名字」，順便把第 2 步那個查詢名字的來歷交代掉。

- **欄位名的出處**：`Aligned to` 來自 v2 `main.bundle.js` 裡的註解
  （`// Raw-id link (e.g. "Aligned to" template short_form)`）與 v3 的
  `$r.push({key:"Aligned To", ..., isAlignedTo:!0})`——**兩個版本大小寫不同**，頁面上註明了。
  其餘欄位名對照 `/docs/website-features/terminfo/` 與 `get_term_info` 的回傳鍵。
- **探針**：worksheet 加 `term_info_fields_pointed_at`，
  把作業單指名的每一欄的**現值**存下來（Name／短名／兩支查詢標籤／Aligned to 列出的編號／
  Licenses／來源資料集）。**欄位改名或內容變了，重跑時 diff 會叫出來**——
  因為這幾欄一變，學員就會照著找不到。
- **型態**：第 15～18 那一串的延伸，但角度不同。前面四條是「起點錯」「數字錯」；
  這一條是**指令不完整**——每一句都對，但學員拿著它到畫面前找不到東西。
  作業單的句子要同時回答三件事：**去哪裡看、看什麼、看到什麼算對。**
  原本的第 4 步三件只答了中間那一件。

### 待辦更新
- [x] `types/tool.md` 已補：新增一節〈滑鼠那一層：一個步驟要同時答三件事〉
      （網域／去哪裡看／看到什麼算對，外加「沒親眼看過的畫面不要寫」），
      〈數字要能重算〉多出第 3 條「『幾筆』是介面的性質」，
      〈稽核〉多出「作業單逐步自查」與「發布前從學員的起點照做一次」兩個習慣。
- [ ] PART 2 起，作業單每一步寫完都用這三件事自查一遍。

### 20. 第 18 條的機制解釋是錯的：大小寫根本不影響畫面
第 18 條我從文件站的 js 推出一套機制（`rows=40`、`Z=8`、依「跟你打的字完全相同」重排），
並照這組參數算出：打 `JRC2018Unisex` 目標排第 0 名、打 `JRC2018unisex` 掉到 40 名以外。
然後把它寫進第 5 步，叫學員故意改大小寫「親眼看它消失」。

**使用者實測：輸入 U 或 u，結果都是同樣的 8 筆。**

- **錯在哪**：我把「照這組參數重跑 SOLR 會怎麼排」當成「畫面上會出現什麼」。
  那組參數是真的（抄自該站的 js），SOLR 對它的回應也是真的（名次 0 vs 40 名外），
  **但兩者之間還隔著我沒看過的東西**——前端實際送出去的查詢字串可能不是我以為的那個、
  中間還有 debounce 與 abort、還有我沒讀到的處理。**重建了五層，第六層還是猜的。**
- **這是同一個錯的第三次**。第 16 條我承認「沒親眼看過畫面」卻照樣寫了筆數；
  第 18 條我以為讀到了原始碼就等於看過，於是又寫了一次——
  **而且這次還更糟，因為我叫學員去做一個會失敗的實驗。**
  第 15、16、18、20 全部是同一個病灶：**用推導出來的東西描述螢幕。**
  `types/tool.md` 那條〈沒有親眼看過的畫面，不要寫〉是第 19 條時補的，
  裡面已經寫了「讀前端原始碼不算看過」——**我補完規則之後，自己違反了它。**
- **怎麼修**：第 5 步的大小寫實驗整段刪掉。剩下的只留**實際操作確認過的兩件事**：
  那個框只給 8 筆、那 8 筆裡沒有 template 本身。
  結尾方框裡引用大小寫當例子的那一句也刪掉。整頁現在沒有任何「大小寫」字樣。
- **探針**：`solr_docsite_palette()` 留著，但加上 `reconstruction_matches_screen: false`
  與 docstring 的警告，並把資料鍵改名為 `spellings_reconstructed_only`；
  另外新增 `confirmed_by_hand`，只放實測確認過的兩條。
  **留著是為了記錄「這條路試過、而且是錯的」，不是給頁面引用的。**

### 待辦更新
- [ ] **這一條要當成硬規矩**：凡是作業單裡「叫學員去做某個操作、然後預期看到某個結果」的句子，
      **在有人實際做過之前不要發布**。推導、原始碼、API 都不能代替。
      第 5 步這種「故意做錯給你看」的設計尤其危險——它預期的是一個**失敗**，
      而失敗有很多種長相，推導出來的那一種不一定是真的那一種。
- [ ] 文件站那個搜尋框到底為什麼不顯示 template，**仍然不知道**。
      不要再從 bundle 推了；要嘛開瀏覽器看，要嘛就別寫。目前選擇後者。

### 21. 整段刪掉：第 5 步與那兩個解釋搜尋框的方框
使用者要求把第 7 節裡談搜尋框的三塊全部刪掉——
第 5 步（選做）、〈為什麼搜尋一個 template 的名字，回來的大多不是 template〉那個 note、
以及〈為什麼這一節一律給網址〉那個 callout。已刪。

- **為什麼這是對的**：那三塊的共同點是**它們都在解釋一個介面**。
  第 20 條刪掉大小寫之後，剩下的內容雖然每一句都有依據
  （8 筆是實測的、「on …」的命名規律是資料本身的性質），
  但整體仍然是「教你怎麼在那個框裡看懂結果」——
  而這一頁的立場已經是**不要用那個框**。留著就是自相矛盾：
  一邊說不要用，一邊花三段教你怎麼用。
  **刪掉之後第 7 節只剩四步，每一步都是一個網址加一個確認點。**
- 開頭方框跟著縮成一句（原本預告「第 5 步會讓你親眼看一次」，第 5 步沒了）。
- **順手補回一件被我弄丟的東西**：這一節在幾輪改寫之間把出處行弄不見了，
  46、58 與 Term Info 各欄的名字一度沒有任何 `out/*.json` 的指向——
  **等於這一節脫離了本課的硬條件而我沒發現**。已補回一行 figsrc。
  這也說明核心那條「改產生器不改產物」在手改 HTML 的頁面上有個缺口：
  出處行是手寫的，刪內容的時候會一起刪掉，而四道稽核都不檢查「每一節有沒有出處行」。
- **型態**：不是錯誤，是**取捨**。但值得記下來，因為判準很清楚：
  **一頁的立場定了之後，凡是跟立場相反方向的內容都要一起清掉，不能只改結論留著論證。**

### 待辦更新
- [ ] `number_audit.py`（還沒寫）之外，再加一項檢查：
      **凡是出現數字的段落所在的那一節，必須有一行 figsrc 指向 `out/*.json`。**
      這次就是靠人眼才發現出處行不見了。

---

## 2026-09-14　number_audit.py

### 22. 硬條件終於有程式把關了（計畫書第 7 節欠了一支，欠到現在）
計畫書第 7 節規劃了 `number_audit.py`，一直沒寫；第 21 條發現第 7 節的出處行被我改掉了
而四道稽核一道都沒響，才動手。寫成兩道：

- **A：每個數字對回 `out/*.json`。** 允許三種變形，因為頁面寫法跟 JSON 存法常常不同：
  四捨五入（`206.51`→206.5）、比例↔百分比（`0.816`→81.6%）、千分位逗號（16127→16,127）。
- **B：含「有辨識度的數字」的每一節都要有一行 `class="figsrc"`。**
  有辨識度＝10 以上或帶小數點。個位數幾乎都是節次、步驟、「三張圖」這類修辭，
  拿來要求出處只會製造雜訊（實測：s5 只有「5」「1 微米」「PART 5」，不該被要求出處）。

**寫完先做破壞測試，因為稽核程式最大的風險是它其實什麼都抓不到：**

| 故意改壞 | 結果 |
|---|---|
| 515 顆 → 5150 顆 | 抓到 |
| 46 列 → 49 列 | 抓到 |
| 刪掉第 7 節的出處行 | 抓到 |
| **46 列 → 47 列** | **沒抓到** |

最後一條是這種稽核的天花板：47 剛好是 worksheet 探針裡 SOLR 的 `numFound`。
**它查的是「這個數字在探針輸出裡出現過沒有」，不是「出現在對的地方」。**
所以加了 `--where`：印出每個數字是在哪幾支探針裡對上的，改完數字用它掃一眼。
天花板寫進 docstring 與 README，不藏著。

### 這一支自己抓到的三件事
1. **`81.6%` 與 `206.5`**：一度以為 out/ 沒記，其實記的是 `0.816` 與 `206.51`
   ——是單位與精度的差，不是缺資料。這就是三種變形的由來。
2. **haystack 一開始太髒**：把網址、雜湊、計時、位元組數、編號裡的數字都算進去，
   489 個數字讓「任何數字都對得上」。清掉之後剩 382，稽核才有意義
   ——**一個什麼都通過的稽核比沒有稽核更糟，因為它給人做完了的錯覺。**
3. 清乾淨之後**立刻多抓到一個**：`1.6`（＝1÷0.626，那個仿射轉換第一軸縮放的倒數）。
   跟 `124`（＝62×2，62 出自引文）、`1.65`（＝886÷536）一起列進 `EXEMPT`。
   **EXEMPT 每一筆都要寫理由**——那就是計畫書說的「對不到的人工逐條交代」，
   只是把它從口頭承諾變成程式裡讀得到的東西。

### 待辦更新
- [x] `number_audit.py`（計畫書第 7 節）——已寫，含 `--where` 與破壞測試。
- [x] 「有數字的那一節必須有出處行」（第 21 條）——已併進 B 道。
- [ ] `ui_claim_audit.py` 還沒寫。不過第 15～21 條之後，這一課的方向已經變成
      **盡量不寫介面描述**，所以它要檢查的東西應該比原本規劃的少很多——
      寫之前先想清楚它到底要管什麼。

### 23. ui_claim_audit.py：不查對錯，只查有沒有寫出座標
第 22 條說「寫之前先想清楚它到底要管什麼」。想清楚的結果是：
**介面描述唯一無法自動驗真假**（程式沒辦法替你去看那個畫面），
所以這一支不查對錯，只查**有沒有把讀者需要的座標寫出來**。四道規則全部對應一次真的卡住：

| 規則 | 來自 |
|---|---|
| A　斷言「畫面上會出現什麼」的那一節要有擷取日期 | 計畫書第 7 節的原始構想 |
| B　給操作指示的那一節要寫出網域 | 第 18 條（把說明文件站當成檢視器） |
| C　不准寫「會看到幾筆」 | 第 20、21 條（筆數是介面的性質） |
| D　叫人移動就要給網址；叫人去看就要指名欄位 | 第 18、19 條 |

**校準花的力氣比寫規則多。** 第一版用「點／欄／畫面／面板」當介面詞，
結果整頁三十幾句誤判——那些字在講圖的方位、講表格的欄、講質心的句子裡到處都是。
改成只認**具名的元件**（Term Info、Queries、Aligned to、搜尋框、檢視器、網址列…），
誤判歸零。**泛稱不能當訊號，這是這支程式最重要的設計決定。**

### 破壞測試抓出的三個真問題
1. **規則 D 第一版太弱**：只檢查「這一步有沒有 `<code>`」。
   但步驟裡本來就會用 `<code>` 包 VFB_00101567 這種編號，
   所以把欄位名整個拿掉照樣過關（實測）。改成**必須有一個裝的不是編號也不是網址的 `<code>`**。
2. **最後一節的範圍會吃到頁尾**，而頁尾有日期。
   於是最後一節等於自動通過所有「這一節有沒有…」的檢查。
   兩支稽核（`number_audit`、`ui_claim_audit`）都有這個洞，一起補：在 `<div class="pager"` 切斷。
3. **`number_audit` 的數字正規表示式有 bug**：為了吃千分位逗號寫成 `\d{1,3}(?:,\d{3})*`，
   結果 `2026` 被切成 `202` ＋ `6`，年份的白名單因此失效。
   第一個分支改成要求**至少一組**逗號。**這個 bug 是 ui_claim_audit 的破壞測試順便照出來的。**

### 這支程式間接抓到的一個正文缺陷
稽核顯示「只有一節有介面描述」，不合理——第 3 節的授權方框明明在講 Term Info 的欄位。
去看才發現：**第 19 條那一輪我對它的修改靜默地沒生效。**
當時用的 `.replace()` 目標字串在第 14 條就被改掉了，而那一行**沒有加 assert**，
所以什麼都沒換；偏偏同一批的另一個 replace 生效了，把原有的「網站的資訊面板」拿掉，
留下一句指涉不明的「**那一欄**我們實測過會變動」。
**我那一輪回報「六處都補上了」，其實只補了五處，還把第六處弄壞。**
已改成「網站上那一欄——**Term Info 的 `Licenses`**——我們實測過會變動」。

- **教訓**：**改檔案的每一個 replace 都要 assert**。這一輪我大部分有加，漏掉的那兩行就出事了。
  而且出事的方式最難發現——**沒有錯誤訊息，只是沒發生**。

### 待辦更新
- [x] `ui_claim_audit.py`（計畫書第 7 節）——已寫，四道規則各有破壞測試。
- [ ] 六道稽核之外，第 20 條那條「叫學員做操作、預期看到某個結果的句子，
      要有人實際做過才能發布」仍然**只能靠人**。程式查得到有沒有寫日期與網域，
      查不到那句話是不是真的。

---

## 2026-09-14　PART 1 全頁複查

六道稽核全過之後，把整頁從頭讀一遍。**稽核全過不等於沒問題**——
以下四條沒有一條是程式抓得到的。

### 24. 正文跟自己的表格矛盾：「三套不是腦」，表上其實有四套
- **錯在哪**：第 3 節寫「十套裡有三套根本不是腦——腹神經索、頭部、前足各一套」。
  但同一節的表上，**腹神經索有兩套**：`VFB_00200000`（JRC2018UnisexVNC，成蟲腹神經索）
  與 `VFB_00100000`（Court2018，成蟲腹神經系統）。所以是四套，而且「各一套」本身就錯。
- **怎麼發生的**：這句話是照計畫書的印象寫的，表是探針產的。
  **一個是記憶、一個是資料，中間沒有人比對過。**
- **改成**：「十套裡有四套涵蓋的不是成蟲腦本身：腹神經索與腹神經系統各一套，
  再加上頭部一套、前足一套。」
- **型態**：新的一型——**正文與同頁的表互相矛盾**。`number_audit` 查的是
  「數字對不對得回 out/」，這裡的「三套」對得回（3 到處都是），
  錯的是它跟旁邊那張表數出來的不一樣。**數字正確不代表敘述正確。**

### 25. 表格裡有一個名字，站上找不到
- **錯在哪**：`VFB_00050000` 那一列的名稱寫「Seymour（L1 ssTEM）」。
  Seymour 不是我編的——它出自該筆的 description：
  `The "Seymour" dataset is a high-resolution 3D electron microscopy dataset…`。
  但**站上 Term Info 的 `Name` 欄寫的是 `L1 larval CNS ssTEM - Cardona/Janelia`，
  裡面沒有 Seymour 這個字。**
- **為什麼要改**：表格那一欄的用途是索引——讓學員拿著它去站上找得到。
  其他列的簡稱（Ito2014、Wood2018、Court2018）都出現在站上的名字裡，
  只有這一列不是。**這跟第 15 條（JRC2018U vs JRC2018Unisex）是同一個病**：
  我們用了一個內部好記、但學員在畫面上對不到的名字。
- **改成**「L1 ssTEM（Cardona/Janelia）」。
- **規矩**：**表格裡當索引用的名字，必須在站上的 `Name` 欄裡找得到。**

### 26. 標點：一句話裡三個破折號
第 23 條補「Term Info 的 Licenses」時，插進一個本來就有破折號的句子，
變成「網站上那一欄——Term Info 的 Licenses——我們實測過會變動——同一個 template…」。
每一段都對，讀起來斷成三截。改用括號收掉中間那一組。
**插入式修補要看整句，不是只看插入點。**

### 27. 首頁對 PART 1 的描述與實際對不上
index 寫「含三張自製的分區地圖與一次自己做的橋接對位」，
實際是六張：三個角度的分區地圖、兩個座標系的對照、一張殘差圖。
（兩張座標框圖是後來為了第 6 節「軸的方向」那一段補的，首頁沒跟著改。）
**改內容的時候，指向那個內容的別頁描述也要一起改**——
這是核心規範第 12 節「跨頁一致性」的實例，而它沒有稽核程式守著。

### 複查時確認沒問題的（留紀錄，免得下次重查）
- 表格 10 列的**分區數與授權**，逐列對回 `out/template_detail.json`，全部一致；
  分區數兩種算法也都一致。
- 橋接那一段的每一個數字（515／5.74／13.57／206.5／81.6%／40／13／103／4.73／5.89／
  0.626・0.605・0.406／282.6／282.3）逐一對回 `out/part1_figures.json` 的 `bridge`，全部一致。
- 「FlyCircuit 兩個 template 版本各佔一半」對得回 `rows_by_template`（25000／25000）。
- 「34×2＋7＝75」對得回 `regions`（paired 34、midline_only 7、labels 75）。
- 跨節指涉（第 3 節說十套、第 4 節三階段、第 6 節回指第 2 節的 FCWB、PART 5 的判準）全部指得到。

### 28. 還沒解的結構性缺口：三層寫法只有一層
首頁承諾「關鍵的每一步都寫三層：網站上怎麼點、同一件事的程式寫法、同一件事怎麼問 AI」，
`types/tool.md` 也把三層寫成這一型的骨架。**但 PART 1 從頭到尾只有第一層。**
頁面上確實每個數字都對得回探針，但那是**我們**跑的程式，不是**給讀者**的程式——
讀者手上沒有任何一行可以自己執行的東西。
這不是筆誤，是 PART 1 寫的時候就沒做，而且拖到現在。
兩條路：補上 PART 1 的程式層與 AI 層，或把首頁的承諾改成「從 PART 2 起」。

**使用者的裁決（2026-09-14）：兩條都不選——「有需要 AI 跟程式時再加入就好，
不用每一 part 都要有」。** 也就是說問題不在 PART 1 少寫了兩層，
在於那個承諾本身訂得太滿。處理見第 29 條。

### 29. 三層寫法降級成工具，不再是義務
照使用者的裁決改了三個地方：`types/tool.md`〈三層寫法〉、`README` 的第二條規矩、
首頁「怎麼教」那一格。新的講法是：**滑鼠是預設；滑鼠做不到或做得很痛苦的時候
（批次、翻頁、算不重複的個數、跨資料集比對），才多給程式層與 AI 層。**

- **順手抓到一個錯的理由。** 舊規範寫「教材的可驗證性由下面兩層撐住」——
  這句話是錯的，而且錯得有後果：照它推論，少了程式層的一節就等於沒有可驗證性。
  **實際上可驗證性來自探針與稽核**：PART 1 從頭到尾只有滑鼠一層，
  但整頁七十幾個數字全部對得回 `out/*.json`，六道稽核也照跑。
  這條錯的理由留著，往後就會有人為了合規而硬加程式層。已改成明講
  「三層不是可驗證性的來源」，並把 PART 1 當成反例寫進規範裡。
- **另一個被這次連帶修正的說法**：舊規範說多寫兩層可以把「會過期的東西關進一個角落」。
  這一輪第 15～21 條證明那不管用——**減少過期風險的辦法是能給網址就不要描述介面**，
  不是多寫兩層去稀釋它。規範裡改成指回〈滑鼠那一層〉。
- **計畫書第 10 節的「決定 1：三層都教」到此被推翻。** 計畫書是 2026-09-13 的討論稿、
  不部署也不進版控，不回頭改；這一條就是它的更新紀錄。
- **型態**：不是錯誤，是**規範訂得比需要的緊**。緊的規範有一個看不見的成本——
  **它會逼人寫出湊數的內容，而湊數的內容不會被任何稽核抓到**。

---

## 2026-09-14　第一次用真的瀏覽器看畫面

**工具**：`scripts/browser_probe.py`（Playwright ＋ 系統的 Chrome 150，不登入、不繼承
`DISPLAY`）。`types/tool.md`〈滑鼠那一層〉第四條「沒有親眼看過的畫面，不要寫」
從今天起有程式可以執行。

### 30. 第 16～20 條那五輪，錯的是同一個字：前端把你打的字轉成小寫

**懸案**：學員在文件站搜尋框打 `JRC2018Unisex`，只看到 8 筆、全長成
「某某 on JRC2018Unisex adult brain」、而且**沒有 `VFB_00101567` 本身**。
第 17 條追到最後寫的是「**我重現不出來**」——照抄的每一組參數，目標都排第一。

**用瀏覽器看一次就解開了，機制有五層，缺一層都不會發生：**

1. 那個框**不是 `<input>`**，是 header 上寫著 `Search VFB… ⌘K` 的一顆按鈕，
   點下去才長出 `#palette-input`。第一版探針因此回報「找不到可見輸入框」。
2. 它打的是 `solr.virtualflybrain.org/solr/ontology/select`，**`rows=40`**。
3. **送出之前一律 `.toLowerCase()`。** 打 `JRC2018Unisex`，攔到的請求是
   `q=jrc2018unisex`。**大小寫不在學員手上。**
4. 同一組參數，只差大小寫：`JRC2018Unisex` 讓目標排**第 1**，
   `jrc2018unisex` 讓它排**第 47**（共 47 筆，分數 424.69 對 58.37）。
   排在它前面的 46 筆，**就是它自己的 46 個分區**——
   每一塊分區的名字裡都帶著母體的名字（`ME on JRC2018Unisex adult brain`），
   於是搜尋母體會把它全部的兒子撈回來，而且兒子的比分比母體高。
5. 第 47 名 > `rows=40`，所以目標**根本沒被拿回來**；
   前端那個「完全相同就提到最前面」的四級重排因此永遠輪不到它。
   畫面再只顯示 8 筆（`Z=8`），看到的就全是分區。

**修法也驗過**：打 `JRC2018U`（Term Info `Symbol` 欄那個短名）→ `numFound=1`，
畫面就一列，正是目標。

- **這件事讀 bundle 讀不出來**，因為 `.toLowerCase()` 那一行跟送出的那一行隔得很遠。
  第 17 條那句「我能做的靜態分析到此為止——剩下只能實際開瀏覽器看」是對的，
  只是當時沒有瀏覽器可以開。
- **`vfb_probe.py` 的 `solr_docsite_palette` 少的就是這一個 `.lower()`。** 補上之後，
  重建與畫面完全一致。原本標著「已被實測推翻，不可用來描述畫面」的那整段註記已改寫。
- **第 20 條的結論（「大小寫根本不影響畫面」）是對的，理由是新的**：
  不是 SOLR 不管大小寫（它很管），是學員碰不到 SOLR。

### 31. 親眼看過畫面之後，PART 1 有六處要改——全部是「欄位名」

一支探針、一輪比對，抓到的比前面四道稽核加起來還多。**共通點：
它們全都是從 API 回傳推出來的欄位名，而 API 的欄位名跟畫面上的欄位名不是同一組。**

| # | 頁面上原本寫 | 畫面上實際是 |
|---|---|---|
| 1 | `Name` 欄寫著 `JRC2018Unisex (VFB_00101567)` | `JRC2018Unisex [VFB_00101567]`——**方括號** |
| 2 | 短名 `JRC2018U`「不出現在 Name 欄」 | 對，但它有自己的一欄，欄名是 **`Symbol`**，就在 `Name` 上面 |
| 3 | 現成查詢在 `Queries` 區 | v2 叫 **`Query For`**；v3 才叫 `Queries (4)` |
| 4 | `Aligned to`（v3 寫成 `Aligned To`） | **兩版都是 `Aligned To`** |
| 5 | `Aligned To` 列的是**編號**（`VFB_00017894` 與 `VFB_00101567`） | 列的是**短名**，而且是一行：`JFRC2, JRC2018U` |
| 6 | 資料集版本看 `Licenses` 欄 | 那是 **`Source`** 欄（`FlyCircuit 1.0 - single neurons (Chiang2010)`）；v2 的 `License` 欄寫的是 `FlyCircuit License` |

- 第 5 條最傷：學員照著找**編號**，畫面上給的是**名字**，而那個名字
  （`JRC2018U`）**又不在第 3 節那張表的任何一欄裡**（表上寫的是 `JRC2018Unisex`）。
  三個名字兩兩對不上，作業單的最後一步等於斷掉。
- 第 23 條那次為了修「指涉不明」，把欄名補成 `Licenses`——**補了一個錯的欄名**。
  v2 是 `License`、v3 是 `Licenses`，而它要指的東西其實在 `Source`。
  **補得出欄名不等於補對欄名**，當時沒有任何辦法可以驗，現在有了。

### 32. 探針自己會說謊，而且不報錯

`type(delay=120)` 對命令面板**會掉字**：面板開啟後還會重新聚焦一次，
前幾個字被吃掉，實際搜尋的是 `C2018Unisex`，畫面顯示 `No match for "c2018unisex"`。
只看 log 會得出「文件站搜尋壞掉」的結論；截圖才看得出輸入框裡是什麼。

- 改成 `fill()` ＋ `assert inp.input_value() == term`。
- **跟第 23 條（`.replace()` 靜默沒生效）是同一個病**：
  **沒有錯誤訊息，只是沒發生。**
- **推論出來的一條規矩**：瀏覽器探針**每一支都要存截圖**。
  JSON 記不下「輸入框裡到底是什麼」「只顯示得下幾列」這種事。

### 待辦更新
- [x] ~~PART 2 主題就是搜尋，在解掉之前不要動筆~~ ——第 30 條解掉了，PART 2 可以寫。
- [x] ~~本課所有介面描述都沒有用瀏覽器親眼看過~~ ——PART 1 的介面描述已逐句核對。
- [x] ~~PART 1 作業單第 2 步的「46 列」還沒在真的介面上點過~~ ——
      `Query For` 區那一列的數字徽章就寫著 46，已寫進作業單當「看到什麼算對」。
- [x] ~~作業單第 3 步（JFRC2 的 58）只改了區塊名，沒有去點過那一支查詢~~
      ——**第 44 條已點過**。
- [ ] v2 與 v3 的欄位名有六處不同（見第 31 條表）。PART 2 之後每一頁寫介面描述時，
      **兩版都要查**——只查一版等於只對一半的讀者成立。
- [ ] `ui_claim_audit.py` 現在只查「有沒有寫出座標」。第 31 條顯示
      **欄位名寫錯它一樣放行**。能不能改成把頁面上的 `<code>` 欄位名
      對回 `out/ui/*.json` 的 `field_order_on_screen`？這會是第七道稽核。

---

## 2026-09-14　PART 2

### 33. 兩道稽核為了 PART 2 改過，兩次都做了破壞測試

- **`number_audit.py` 的「原文」範圍擴大**：原本只掃 `out/*.json`，現在加掃
  `out/ui/*.json`。理由是畫面探針的輸出跟 API 探針的輸出一樣可以重跑對回去，
  而 PART 2 講的「同一個字在三個框給三種列數」那些數字**只存在於 out/ui/**。
- **`ui_claim_audit.py` 的規則 C 從「全面禁止寫筆數」改成「有條件放行」**：
  那一節的出處行要**指名某一支 `out/….json`**、而且要有擷取日期，才可以寫筆數。
  當初全面禁止，是因為**當時沒有任何辦法驗證畫面上有幾列**——唯一的數字來源是
  API 的筆數，而那是另一回事（第 16 條就是這樣錯的）。現在有瀏覽器探針了。
  - **放寬過的規則一定要做破壞測試。** 三種破壞各試一次：拿掉出處行、拿掉日期、
    出處行只寫「由探針查得」不指名檔案——三種都照樣被抓出來（各 3 筆）。
  - 放行條件寫成「指名到檔案」而不是「提到 out/」，是因為
    **「由探針查得」這種出處行等於沒有出處**：重跑的時候沒有人知道要比對哪一支。

### 34. 差點又犯第 31 條那個錯，這次在寫進去之前被擋下來

PART 2 作業單第 3 步的草稿寫「<code>Aligned To</code> 寫著 <code>JRC2018Unisex</code>」
——那是照 API 的 `Images` 鍵推的。**動手拍一張才發現畫面上寫的是 `JRC2018U`（短名）。**

- 這跟第 31 條第 5 項是**同一個錯**：`Aligned To` 列的是 Symbol，不是 Name，也不是編號。
- 差別只在於這次是**寫之前**查的，不是發布之後被使用者抓到的。
- **可以固定下來的作法**：作業單每寫一步，就在 `browser_probe.py` 加一支對應的探針，
  把那一步要學員看的欄位現值存下來。PART 2 的四步對到四支
  （`terminfo_v2_region`、`terminfo_v2_individual`、`palette`、`search_v3`）。
  這樣「發布前有人照著做一次」那條規矩就不再靠記性。

### 35. 交叉檢查又抓到兩件事（都不是看出來的，是比出來的）

1. **`get_term_info` 的 `Examples` 欄不穩定。** 同一個 id 連問五次，
   交替回 3 套與 4 套（少的那次少了 Ito2014）。**這是第 12 條（授權欄不穩定）的第二例**
   ——同一支 REST 的不同欄位，同一種病。個數一律改用知識庫的 Cypher 算。
   （`ListAllAvailableImages` 則穩定，連問五次都是 4 筆、同樣四個 id。）
2. **PART 1 第 3 節的表上有一列的名字，站上 `Name` 欄找不到。**
   那一列寫 `JRCFIB2018Fum`，但站上 `Name` 是 `JRC_FlyEM_Hemibrain`——
   `JRCFIB2018Fum` 是它的 `Symbol`。**第 25 條那條規矩（表格裡當索引用的名字，
   必須在站上的 `Name` 欄裡找得到）自己被違反了一次，而當時沒有人發現**，
   因為那張表是拿 REST 的頂層 `Name` 產的，而**REST 的頂層 `Name` 其實是畫面上的 `Symbol`**。
   十套裡有五套兩者不同，三套完全對不上。已改，並在表的圖說裡指向 PART 2 第 4 節的對照表。

### 待辦更新
- [x] ~~PART 2~~ ——已完成，七節 ＋ 四步作業單，`part2-names.html`。
- [x] ~~作業單第 3 步（JFRC2 的 58 列）只改了區塊名，沒有去點過那一支查詢~~
      ——**第 44 條已點過**，並補上點完之後的標題字串。
- [ ] `ui_claim_audit.py` 仍然查不到「欄位名寫錯」。第 31、34 條都是人去比對出來的。
      可行的作法：把頁面上 `<code>` 包起來的欄位名，對回 `out/ui/*.json` 的
      `field_order_on_screen`。**這一條現在有材料可以做了**——六支畫面探針都存了那個欄位。
### 36. 寫 _notes 的時候抓到自己剛寫進頁面的一句過寬的話

PART 2 第 5 節原本寫「真要用搜尋框就用 v3，**它給的列最完整**」。
依據是 v3 取回 150 個詞、文件站只要 40 筆——但**同一份探針裡就寫著 v2 取回 494 個詞**，
比 v3 多。「最完整」這個排名沒有任何證據，是順手寫下去的。

- 已改成可驗證的說法：**避開文件站那一個**（只有它不展開萬用字元、只要 40 筆），
  另外兩個都展開，並把兩個實際的詞數（494 與 150）寫出來，讓讀者自己比。
- **型態**：規範第 3 節那張表的最後一列，「把推論寫成事實」。
  而且它躲過了全部四道稽核——數字都對得回探針，錯的是**用那些數字下的排名**。
- **抓到它的方式值得記**：是在把這一輪的事情寫進 `_notes` 的時候，
  逐條重讀自己剛寫的句子才發現的。**寫修正紀錄本身就是一道稽核。**

---

## 2026-09-14　部署環境的一條待辦（不是錯，是會到期的東西）

### 37. GitHub Actions 警告：四個 action 還在指定 Node.js 20

2026-09-14 那次部署（run `34803867421`）成功，但留下一條警告：

> Node.js 20 is deprecated. The following actions target Node.js 20 but are being
> forced to run on Node.js 24: `actions/checkout@v4`, `actions/configure-pages@v5`,
> `actions/deploy-pages@v4`, `actions/upload-artifact@v4`.

- **現在不影響任何東西**：runner 直接拿 Node 24 跑它們，頁面照常發布。
  記在這裡是因為「被強制升版」跟「支援」是兩回事，遲早會變成硬錯誤。
- **`.github/workflows/pages.yml` 裡實際寫的是四行**：`actions/checkout@v4`、
  `actions/configure-pages@v5`、`actions/upload-pages-artifact@v3`、`actions/deploy-pages@v4`。
  警告裡點名的 `actions/upload-artifact@v4` **不在我們的檔案裡**——
  它是 `upload-pages-artifact@v3` 內部呼叫的。**所以升版時要升的是 `upload-pages-artifact`，
  不是去找一個我們根本沒寫的 `upload-artifact`。**
- **要做的事**：等上游釋出改用 Node 24 的版本就把這四行一起升。
  升完跑一次 `workflow_dispatch` 確認部署仍然成功，並照老規矩驗三件事——
  首頁 200、`out/ui/` 拿得到、而 `_notes/`、`vfb_plan.docx`、`make_vfb_plan.py` 是 404
  （那三樣是由 workflow 裡的 `rm -rf` 擋掉的，**升版最可能弄壞的就是那一步**）。
- **型態**：這一條跟教材內容無關，是**部署環境的時效**。
  `types/tool.md` 的〈這一型的時效〉分了「介面改版」與「資料換版」兩種，
  這是第三種：**我們自己的建置環境也會過期**，而它不會被任何一道稽核掃到。

---

## 2026-09-14　PART 3

### 38. 卡最久的那個待辦解開了，而且答案早就寫在 PART 2 裡

**待辦原文**（探針階段就記下的）：「`ListAllAvailableImages` 對 medulla 只回 4 筆，
但 `ImagesNeurons` 回 226,524 筆——差別要弄懂，**寫 PART 3 前要弄懂**。」

答案是**這三支查詢問的是三種不同的東西**，而分野正是 PART 2 那條類別／個體：

| 查詢 | 回什麼 | 編號 | 筆數 |
|---|---|---|---|
| `NeuronsPartHere` | 神經元的**種類** | `FBbt_` | 471 |
| `ImagesNeurons` | 一條一條重建出來的神經元（**個體**） | `VFB_` | 226,524 |
| `ListAllAvailableImages` | 這個**腦區自己**被畫出來的那幾塊 | `VFB_` | 4 |

- **驗證方式是抽樣看編號，不是讀文件**：前者回 `FBbt_00110069 Dm8b`，
  後者回 `VFB_jrmc10fc T4a_R (MaleCNS:100855)`。一看編號開頭就分出來了。
- **教訓**：這個待辦掛了很久，是因為當時把它當成「兩個數字對不上」的問題。
  它其實是「這兩支查詢在問什麼」的問題——**而那個答案在寫 PART 2 的時候就已經有了**。
  核心第 4 節那條「兩個數字對不上時，先確認它們量的是不是同一件事」，
  這一次的具體形狀是：**先問兩支查詢回的編號開頭一不一樣。**

### 39. 我在探針註解裡寫「不是巢狀」，被自己的資料當場推翻

`query_overlaps` 探針的第一版註解寫著「**這幾支查詢不是巢狀的，也不能相加**」。
跑完看輸出：`synaptic ⊆ part`、`pre ⊆ synaptic`、`post ⊆ synaptic` **三個都是 True**。

- 正確的說法是「**是巢狀的，但不能相加**」——不能相加的理由是
  前突觸與後突觸重疊 230 種、而且兩者聯集 365 還填不滿「有突觸終端」的 464（差 99 種）。
- **型態**：跟第 36 條同一型——**把推論寫成事實**，而且寫在探針的註解裡，
  離頁面只有一步。差別是這次寫完當場就跑了，所以三十秒後就被推翻。
- **可以固定下來的作法**：探針的註解**要在跑完之後回頭再讀一遍**。
  探針是先寫註解、後看輸出的，那個順序天生會讓註解跑在證據前面。

### 40. 追機制追到一半就停了，而且頁面照這個界線寫

`NeuronsSynaptic` 回 464，我想知道它到底走哪幾條關係，於是用知識庫重建：

1. 先猜關係名（`has_presynaptic_terminals_in` 之類）——**回 0，名字是錯的**。
2. 列出所有指向 medulla 的關係型別，拿到真的名字
   （`sends_synaptic_output_to_region`、`receives_synaptic_input_in_region`、
   `has_synaptic_IO_in_region`…）。
3. 沿 `SUBCLASSOF` 繼承重算 → 250；再把 medulla 底下的次分區也算進去 → 還是 250。
   **離 464 還差 216。**

**所以頁面上不寫機制。** 寫的是量得到的東西：包含關係、重疊數、以及那 99 種的編號。
規範第 3 節那條「不要無中生有的論證」在這裡的具體形狀是——
**我有一個聽起來很合理的解釋（關係型別不同 ＋ 繼承），但它只解釋得了 56/99，所以它不能寫。**

- 順帶確認了一件真的：`TmY6` 自己沒有掛任何跟 medulla 有關的標註，
  是從父類 `transmedullary Y neuron` 的 `has_synaptic_IO_in_region` 繼承來的。
  **繼承這件事是驗過的，機制的全貌不是。** 頁面只用前者。
- 中途還推翻過自己一個假設：「那 99 種多半是上位類別」——實際上 84/99 是末端類別。
  **幸好有數，沒有直接寫『多半』。**

### 41. 探針量到「篩選沒作用」，其實是我抓錯輸入框

`query_results` 探針量篩選行為，第一次的結果是「打了字，列數 10 → 10，沒有變化」。
差一點就寫成「這個篩選框只是裝飾」。

- **原因**：`input[placeholder*='Filter']` 同時對到兩個框——
  Layers 面板那個 placeholder 是 `Filter`，結果表那個是 `Filter Results`，
  而 `.first` 抓到的是前者。**篩錯框，不是篩選沒作用。**
- 改成 `placeholder='Filter Results'`、並且用 `type()` 送真的按鍵（`fill()` 不觸發它的篩選），
  量到的是 **10 → 1、期間 0 個網路請求**。
- **跟第 32 條同一個病的第三次**：**沒有錯誤訊息，只是量到別的東西**。
  前兩次是 `.replace()` 沒生效、`type()` 掉字。
  **這一次的教訓更窄：選擇器用 `*=` 模糊比對時，要先確認它只對到一個。**

### 42. 規則 C 的有條件放行，第一次真的被用上

PART 3 第 4 節寫了「列數從 10 列變成 1 列」——這是**畫面上的列數**，
放在半天前還是違規的。現在它通過，是因為那一節指名了 `out/ui/query_results.json`
並標了擷取日期。**這就是當初放寬那條規則要換到的東西**：
學員最常撞到的「篩完之後標題數字沒變」這件事，終於寫得出來。

### 43. 寫 _notes 又照出一句超出證據的話（第二次）

PART 3 第 5 節原本寫「**顏色是你自己挑的，不帶任何意義**」。
我驗到的只有一件事：`Layers` 每一列有一個 `Color` 控制按鈕。
那支持「顏色可以自己換」，也支持「顏色不是資料的一部分」——
但**不支持「預設的那個顏色也不帶意義」**，而原句把兩件事包在一起說了。

- 已改成：先寫驗到的（每一列的顏色都可以自己換），再寫推得的
  （所以畫面上的顏色不是資料的一部分），中間用「由此推得」把兩者分開。
- **這是第 36 條之後第二次「寫修正紀錄的時候抓到自己剛寫進頁面的話」。**
  兩次都是同一個位置——**正文裡從一個觀察直接跳到一個通則**，中間那一步沒寫出來。
  值得當成固定的收尾動作：**寫 `_notes` 的時候把頁面上每一句斷言的依據唸一遍。**

### 待辦更新
- [x] ~~寫 PART 3 前要弄懂 `ListAllAvailableImages` 與 `ImagesNeurons` 的差別~~ ——第 38 條。
- [x] ~~PART 3~~ ——已完成，七節＋四步作業單，`part3-queries.html`。
- [ ] `NeuronsSynaptic` 的 464 還原不出來（第 40 條）。寫 PART 4 會再遇到同一類問題
      （連線數怎麼算），到時候要嘛還原成功、要嘛也照這次的界線寫。
- [ ] 第 36、43 條同一型出現兩次。收尾動作固定成：**寫 `_notes` 時把頁面上
      每一句斷言的依據唸一遍**，特別是「從一個觀察跳到一個通則」的句子。

---

## 2026-09-14　把 JFRC2 那支查詢真的點了一次

### 44. 全站最後一個沒操作過的步驟，點下去就抓到兩件事

從第 31 條掛到第 43 條的那條待辦：**PART 1 作業單第 2、3 步只驗過徽章上的數字，
沒有人真的把查詢點下去。** 新增 `browser_probe.py` 的 `painted_domains` 探針，
兩套 template 各做一次完整的「開頁面 → 找 `Query For` → 點 → 讀結果表標題」。

**數字是對的**（這部分沒事）：

| | `Query For` 那一行 | 點下去之後的標題 |
|---|---|---|
| JRC2018Unisex | `46Painted domains for JRC2018U` | `46 List all painted anatomy available for JRC2018Unisex` |
| JFRC2 | `58Painted domains for JFRC2` | `58 List all painted anatomy available for adult brain template JFRC2` |

**但抓到兩件作業單沒寫、而學員一定會撞到的事：**

1. **點下去之後，標題不是你剛剛點的那個名字。**
   標籤是 `Painted domains for JFRC2`，標題卻是
   `List all painted anatomy available for adult brain template JFRC2`——
   **兩句完全不同的話。** 而且更混亂的是：**標籤用 `Symbol`（短名），標題用 `Name`（正式名）**
   ——JRC2018U 那一套尤其明顯，點的是 `…for JRC2018U`，跳出來的是 `…for JRC2018Unisex`。
   學員很容易以為自己點錯了。兩步都補上了可以逐字比對的標題字串。
2. **結果表的欄位是跟著查詢走的。** 這支 `Painted domains` 的結果表是
   `Name`／`Type`／`Definition`／`Images`**四欄**，
   而 PART 3 第 3 節那支神經元查詢是**五欄**（`Name`／`Gross_Type`／`Template_Space`／
   `Imaging_Technique`／`Images`）。PART 3 原本寫「表頭有五欄」，讀起來像通則——
   已改成「**這支查詢的**表頭有五欄」，並在該處舉這一支當對照。

- **這條待辦掛了那麼久，代價就是這兩件事一直沒被發現**，
  而它們兩個都是「照著做會卡住」的那一類。**`types/tool.md` 那條
  「發布前要有人從學員的起點照著做一次」，這一次證明它不是形式。**
- **現在它有程式了**：`painted_domains` 探針就是那一步的執行者。
  規矩可以收得更緊——**作業單裡凡是「點下去」的步驟，都要有一支探針真的點下去，
  並把點完之後畫面上的字串存起來**。只讀徽章不算做過。

### 待辦更新
- [x] ~~PART 1 作業單第 3 步（JFRC2 的 58 列）沒有去點過~~ ——第 44 條，兩步都點過了。
      **全站的作業單步驟至此全部實際操作過。**

---

### 45. 使用者抓到的：「那四支查詢」沒有指名是哪四支

**使用者的原話**：「『那四支「神經元」查詢的數字看起來像一層層篩下去。』講清楚是哪四支」。

PART 3 第 2 節原句沒有指名。查下去發現有兩層，**第二層比第一層嚴重**：

1. **沒指名是哪四支**——而上面那張表裡跟神經元有關的其實有**五**支
   （`Images of neurons…` 回的是個體）。「那四支」要讀者自己挑掉一支。
2. **整段用的三個中文短名，從來沒有跟表上的英文標籤對應過。**
   「前突觸 ⊆ 有突觸終端 ⊆ 有一部分在裡面」這句話裡三個詞都是我自己造的簡稱，
   讀者只能猜它們各對應表上哪一列。**這才是真正讀不下去的原因。**

改法：先講清楚五支裡為什麼有一支先放一邊，再用一張三欄小表把
「本頁往後這樣稱呼它 ／ 站上的查詢 ／ 回幾種」綁在一起，之後整段只用短名。

- **這一條同時犯了規範的兩處**：第 10 節第 5 道（指涉詞要指名對象）
  與第 6 節（術語第一次出現就給一次別名，之後只用一個）。
- **六道稽核一道都沒響**，因為這兩條都是人工抽查、沒有程式在守。
  跟「欄位名寫錯抓不到」（第 31、34 條）是同一類缺口：
  **稽核管得住「有沒有出處」，管不住「讀不讀得懂」。**
- **為什麼自己複查時沒抓到**：我寫的時候腦子裡有那張表，
  所以「前突觸」讀起來完全清楚。**造簡稱的人是最不可能發現簡稱沒綁定的人。**
  這也是為什麼這一類只能靠程式抽出來、或靠別人讀。

---

### 46. 使用者抓到的：「拿到你的清單」——是 1 列還是 471 列？

**使用者的原話**：「『最後按結果表底下的 Download results (CSV)，拿到你的清單。』
寫清楚是拿到一個還是 471 個的清單?」

**我不知道**——作業單第 3 步才剛叫學員把結果篩成一列，第 4 步就叫他按下載。
推不出來，只能真的按下去。按了兩次（一次不篩、一次篩成一列）：

**兩次都是 471 列，連檔名都一樣**（`471_Neurons_with_some_part_in_medulla.csv`）。
**篩選完全不影響下載。** 已寫進第 6 節與作業單第 4 步。

順帶照出**第三組欄名**：CSV 的表頭是 `id,name,gross_type,template,technique,images`
——跟畫面上的（`Name`／`Gross_Type`／`Template_Space`／`Imaging_Technique`／`Images`）
和 API 回的鍵（`label`／`tags`／…）都不一樣。而且 **CSV 比畫面多一欄 `id`**，
值還帶著 `[Dm8b](FBbt_00110069)` 這種連結語法。三組欄名已並排成一張表寫進第 6 節。

- **型態**：作業單的「看到什麼算對」漏掉了**下載這個動作的結果**。
  前面的步驟都有可比對的字串，只有最後一步寫成「拿到你的清單」——
  **那是一句沒有驗收條件的話。**
- 已加 `csv_export` 探針。**這是第 44 條那條規矩的第二個實例**：
  作業單裡每一個動作，都要有一支探針真的做一次。

### 47. 第七道稽核：`field_audit.py`

把頁面上 `<code>` 包起來的介面名字，對回探針拍到的字串。結果分三桶：
**畫面上有** ✓ ／ **只有 API 有** ⚠ ／ **兩邊都沒有** ✗。
中間那桶是重點——**那正是第 31 條那六個錯躲藏的位置**。

第一次跑就抓到東西：

- **兩個真的缺口**：`Filter Results` 與 `Slice Viewer` 頁面上寫了，但探針跟它們
  互動過卻沒把字串存下來。已補進 `query_results` 與 `viewer_state`。
- **三個外推**：`JRCVNC2018U`、`JRCFIB2018Fum`、`COURT2018VNS` 是拿 API 推的
  `Symbol` 值，畫面沒拍過。加了 `template_symbols` 探針把五套都拍一遍——全部一致。
- **一句話寫得有歧義**：PART 2 寫「不是用**介面的** `Examples` 欄」，
  而那是 REST 回的欄位、畫面上根本沒有這一欄。已改成「REST 回的」。

**設計上踩的兩個坑**（都是破壞測試照出來的）：

1. **出處行要整段排除。** 出處行裡的 `<code>` 裝的是檔名、探針名、JSON 的鍵
   （`worksheet`、`docsite_search_box`），不是介面名字。不排除的話，
   **出處行寫得愈仔細雜訊愈多——那等於處罰好習慣**（雜訊從 9 降到 3）。
2. **對照集裡不能有我們自己的中文註解。** 破壞測試把 `Aligned To` 改回錯的
   `Aligned to`，稽核只給了 ⚠ 而不是 ✗——因為 `out/worksheet.json` 有一個**中文的鍵**
   「第4步：Term Info 的 Aligned to 欄」，裡面就抄著那個舊的錯名字。
   （而那個鍵本身也是第 31 條漏改的：改了頁面沒改探針。兩件事一起修了。）
   **規則：含中文的字串一律不進對照集。** 服務端的東西是英文，我們的註解是中文。

破壞測試：把 `Aligned To` 改回 `Aligned to` → ✗；把 `Query For` 改成 `QueryFor` → ✗。

### 48. 第八道只成了一半：`refs` 留下、「未綁定的簡稱」兩種設計都失敗

**`refs`（指涉詞）成了**：掃「那N支」「上面那」「上表」「下圖」這類話，
印出前後 60 字讓人問一次「它指的東西，在這一句裡叫得出名字嗎」。
拿第 45 條那個真實案例測，**抓得到**（`【那四支】`）。

**「未綁定的簡稱」失敗，兩種設計都試過，已移除**：

1. **只看 `<b>` 包起來的短詞**——抓不到。因為那三個簡稱在頁面上是
   **夾在句子和引號裡的裸文字**，唯一一次被 `<b>` 包住是三個連在一起
   （`前突觸 ⊆ 有突觸終端 ⊆ …`），被「整個元素只有一個詞」的條件篩掉了。
2. **改掃正文裡重複出現的中文短詞**——38 個候選幾乎全是雜訊
   （「個數字」「到的是」這種**跨詞碎片**）。沒有斷詞的 n-gram 做不了這件事。
3. 修正版（從 `<b>` 與「」取候選、先拆開再比對）**加不加綁定表輸出完全一樣**。
   追下去才發現原因：**§2 那張十支查詢的表本來就有「在這裡有前突觸終端的種類」**
   ——所以 `前突觸` 早就算「出現在表格裡」了。

**真正的結論比工具重要**：第 45 條我判定「簡稱沒綁定」是**判重了**。
鬆散的綁定本來就在（表格的中文描述欄），缺的只有「那四支是哪四支」——
而那個 `refs` 抓得到。**留著一個證明不了價值的稽核，比沒有更糟**：
它會讓人以為這一類有程式在守。

---

### 49. FANC 解掉了，而且答案比預期精確：八套的判準是一個可驗證的等式

**第 7 條的原文**（探針階段就記下，掛了最久）：「文件站列了 FANC，但
`/list_connectome_datasets` 只有 `catmaid_fafb` 與 `catmaid_l1em`。
是被移除了，還是它不算 connectome dataset？**寫 PART 4 之前要查。**」

查到的是一整套機制，比原本的問題大：

1. **FANC 在知識庫裡有站台也有神經元**：`catmaid_fanc`（VFB CATMAID Adult VNC）、
   資料集 `Maniates_Selvin2020` 底下 **2,029 顆**神經元。
2. **但它一條連線都沒有**（0 條 synapsed_to 邊）。對照 FlyWire：139,255 顆、7,595,968 條。
3. **知識庫給每個站台掛三個旗標**：`dense`（整幅密集重建）、`is_data_source`（現行版本）、
   `deprecated`（明確退役）。
4. **`dense ∧ is_data_source` 算出來剛好就是 REST 回的那八套，不多不少。**
   FANC 有 `is_data_source`、沒有 `dense`，所以不在清單裡。

**那個等式現在每次重跑探針都會再驗一次**（`eight_equals_dense_and_current`）。

- **這條待辦掛了那麼久，是因為我一直把它當成「FANC 在不在」的是非題。**
  它其實是「那份清單是怎麼選出來的」——**問清楚選擇規則，順便就答完了原本的問題，
  而且答案可以重跑。** 這跟第 38 條（4 筆 vs 226,524 筆）是同一型：
  **看起來是兩個數字對不上，實際上是「這份東西在回答什麼」沒問清楚。**
- 順帶分出兩種退役狀態，PART 4 第 4 節分開講：
  **舊版但還在**（`dense`、無 `is_data_source`，**連線還在**：BANC v626、male-cns v0.9）
  與**明確退役**（`deprecated`，**連線被拿掉**：只有 hemibrain v1.0.1）。
- **一個沒解釋、也不打算解釋的現象**：BANC 從 v626 到 v888，神經元從 80,003
  掉到 64,546，**少了 15,457 條**。頁面明講「本課不解釋為什麼——那要問做那份重建的人」，
  只講操作上的後果（換版數字會變，而且可能往下變）。
  這是第 40 條那條界線的第二次使用。

### 50. 第七道稽核第一次在新頁面上發揮作用

寫 PART 4 時 `field_audit.py` 叫出 `is_data_source`——**頁面指名了三個知識庫旗標，
而探針沒把旗標名存下來**，等於那個宣稱無從追回。已在 `connectome_overview` 補存
`site_flags`（逐站三個旗標的值）與 `flag_meanings`。

- **這正是這道稽核想擋的形狀**：不是寫錯，是**寫了一個沒有證據鏈的名字**。
- 同一輪 `number_audit` 也叫出兩件：15,457 是頁面上當場相減的（已列 EXEMPT 並寫理由）、
  第 6 節那張總結表沒有出處行（它引用的都是前面出現過的數字，但**引用也要指得回去**，
  已補一行寫明每個數字各出自哪一支探針）。
- `content_audit refs` 叫出兩處該指名而沒指名的：「見上一節」與
  「在 medulla 那張表上長什麼樣」。都改成 `第 N 節那張表`。
  **這一道是第 45 條之後新加的，第一次用在新頁面上就抓到東西。**

---

### 51. 「拿原始資料驗證 VFB」第一次真的做了，結論要講得很小心

PART 5 是全課唯一做得到這件事的一部分（本機有 FlyCircuit 全套，28,573 個 SWC）。
把 `Cha-F-100205` 的兩份骨架擺在一起量：

| | FlyCircuit 釋出 | VFB 下載 |
|---|---|---|
| 點數 | 48 | 49 |
| 分支點 | 5 | **4** |
| 端點 | 7 | **6** |
| 半徑欄 | 全部是 0 | 全部是 `NA` |
| 產生工具 | TREES toolbox | `nat::write.neuron.swc` |

**分支點的個數是拓樸量，換座標系不會變**——所以 VFB 那一份不是原始那一份的座標轉換。

- **結論的措辭改過三次才定。** 不能寫「VFB 錯了」：VFB 從未宣稱它提供的是
  FlyCircuit 釋出的那個檔案。頁面最後寫的是**操作上的後果**：
  「你在 VFB 上看到的分數是拿 VFB 那份骨架算的；你拿 FlyCircuit 的 SWC 自己重算，
  不會得到同一個數字。」**這是可驗證的，而「誰對」不是。**
- **順帶量到一件對 NBLAST 有影響的事**：兩份的半徑欄都沒有數值，
  所以**粗細沒有進到形態比對裡**。

### 52. 差點判錯：邊集合不能拿來判拓樸

比 VFB 那兩個座標系的版本時，我先用**無向邊集合**比，得到「兩版拓樸不同」，
而且已經準備寫進頁面。

**那是錯的。** 兩版的節點編號不一樣（同一棵樹用不同順序寫出來），
邊集合當然不同。改用**度數序列**比，結果是**完全相同**——同樣 49 點、4 分支點、6 端點。

- 頁面上現在寫的是「形狀統計一模一樣，長度差 3.8%」，並解釋那正是 PART 1
  講的橋接：**樹的結構原封不動，每一段的長度被非剛體對位拉扯過。**
- **教訓**：比兩份資料的結構時，**先問這個量對「書寫順序」敏不敏感**。
  邊集合、parent 序列都敏感；度數序列、點數、分支點數不敏感。
  敏感的量拿來比，比出來的是格式差異，不是內容差異。
- 這跟第 39 條是同一型（**我在探針裡寫了一個結論，被自己的資料推翻**），
  只是這次推翻它的是我自己換了一個量。

### 53. 三個小東西，都是「寫死的清單會靜默吃掉新東西」

1. **欄位白名單**：`nblast_results` 探針的欄名用白名單篩，結果把畫面上真的有的
   `Type` 與 `Score ▼` **靜默丟掉**——而 `Score` 正是 PART 5 的主題。
   改成「排除 Layers 面板固定那幾欄，其餘照收」。
   **這正是「欄位是跟著查詢走的」這件事最容易被漏掉的方式。**
2. **`has_radius` 這個欄位名會誤導**：本機那份有半徑欄但全是 0，
   `np.isfinite(0)` 是 True，於是回報「有半徑」。改成直接講是哪一種沒有
   （`全部是 0` ／ `全部是 NA`）——**布林值把兩件不同的事壓成一件。**
3. **`number_audit` 把目錄當內文**：目錄會把各節小標再列一次，
   而 PART 5 的小標裡有「48 點對 49 點」，於是「前言」永遠缺出處行。
   整段跳過 `<nav class="toc">`（目錄是索引不是內容）。改完做了破壞測試，
   刪掉第 1 節的出處行照樣抓得到。

---

### 54. PART 6：計畫書列的四件必查，四件都跟計畫書寫的不一樣

計畫書第 8.2 節列了四件「寫對應的 PART 之前要親自確認」。全部查完，**四件都有出入**：

| 計畫書寫的 | 實際 |
|---|---|
| MCP「對外開七類工具」 | **11 個**。七類都在，另外四個文件沒提，而且全跟 PART 4 的主題有關（`list_connectome_datasets`、`query_connectivity`、`get_predicted_neurotransmitters`、`get_known_neurotransmitters`） |
| `vfb-connect` 的 import 路徑（要確認還能不能跑） | **2.4.5，舊路徑仍可用**，頂層 `from vfb_connect import VfbConnect` 也可用。但計畫書沒寫兩件事：**回傳是 DataFrame**、**`import` 本身就連線**（5 秒、600 字元橫幅） |
| 「bulk download 圖示」，四種格式 | 格式沒錯，**但三個名字都不一樣**：圖示 title 是 `Download Contents`、對話框叫 `Download Data`。而且第二段挑的是**要下載哪幾個東西**，不是計畫書寫的「挑欄位」 |
| FlyCircuit 16,127 顆與本機的逐筆對應 | **16,052 對得上（99.5%）**。只在 VFB 的 75 個裡抽查 20 個有 12 個在本機別的資料夾找得到；**只在本機的 12,521 個命名方式一模一樣**——本機那套就是比 VFB 多一倍 |

**「文件是導覽，不是實作」這一課到此是第五次。** 頁面上把前四次一起列出來當對照。

### 55. 三層寫法在這一 PART 是內容，不是湊數——而且它逼出一個真正的發現

第 29 條把三層寫法降級成「需要時才用」。**PART 6 是唯一名正言順該寫三層的一頁**，
因為它的主題本身就是那三層。而照 `types/tool.md`〈跨頁一致性〉那條
「三層要拿得到同一個答案」去做，跑出來的結果比預期好：

    滑鼠 471　MCP 471　程式 510（預設）／481（cells_only=True）

**三個差別全部來自參數的預設值**，一個算錯的都沒有：

1. **510 → 481**：`get_terms_by_**region**` 回的是「跟這個腦區有關的所有詞」，
   多出來的 39 個是**次分區**（`medulla layer M1`–`M10` 那些），不是神經元。
2. **481 → 471**：多出來的是 **9 個膠細胞**——`cells_only` 的 cell 包含膠細胞，
   而 `NeuronsPartHere` 只要神經元。**cell 不等於 neuron。**
3. **反過來，471 裡有 1 個是程式那層沒有的**：`FBbt_00110069`，
   標籤是 *obsolete distal medullary amacrine neuron Dm8b*——**一個已棄用的詞**。
   `vfb-connect` 預設 `include_deprecated=False` 會排掉它，站上那支查詢沒有這個預設。
   而它的新版（`FBbt_00053638`／`FBbt_00053639`）反而只出現在程式那一層。

- **第 3 點順帶抓到 PART 3 的一個瑕疵**：PART 3 第 1 節拿 `Dm8b` 當「神經元的種類」的例子，
  而那個 `Dm8b` 正是已棄用的那一個。已換成 `Tm19`。
  **舉例子的時候要看一眼那個例子本身是不是好的**——它確實是查詢回的第一列，
  但「查詢會回它」跟「它適合當例子」是兩件事。
- 這一節也給了 AI 那一層一個具體的風險說法：**它會替你做決定，而且不會告訴你**。
  上面那三個決定（次分區算不算、膠細胞算不算、棄用的詞算不算），
  用滑鼠時決定的人是 VFB、用程式時是函式的預設值、用 AI 時是 AI——**三種都不是你**。
  所以作業單第 3 步的重點不是讓 AI 給答案，是**追問它用了哪個工具、填了哪些參數**。

### 56. 第七道稽核第二次抓到「沒有證據鏈的名字」

`field_audit` 叫出 11 個 MCP 工具名——頁面列了它們，而 `three_layers.py` 只存了
`run_query` 的回傳鍵，沒存工具清單。已補存（含每個工具的參數與必填欄位，
以及「文件說的七類是不是都在」這個布林值）。**跟 PART 4 那次（旗標名沒存）是同一型。**

順帶調了這道稽核一次：**以 `()` 結尾的 `<code>` 不算介面名字**（`VfbConnect()`、`len()`
是函式呼叫）。只擋結尾——`Mesh (OBJ)` 那種括號在中間的仍然要查。

---

### 57. 使用者提議搬一段，追下去發現 PART 3 本來就寫錯了

**使用者的原話**：「[`Download Data` 那一段] 也加到前面去，比如說加在 part 3 的匯出，
或是你可以建議其他地方，討論一下」。

討論時才發現，搬不搬是次要的——**PART 3 第 6 節原本那句話本身就是錯的**：

> CSV 裡是結果表那幾欄，不是影像。**影像要另外一顆一顆下載。**

而 `Download Data` 對話框的第二段就是**可以一次勾好幾個**（`All Instances` 或逐個挑）。
**PART 3 告訴讀者做不到的事，PART 6 才說做得到。**

- **為什麼寫 PART 3 時沒發現**：那時我還沒去點過打包下載那顆圖示——
  它在計畫書裡是 PART 6 的內容，所以我照著計畫書的分工走，沒有問
  「我現在寫的這句話，會不會被後面那一 PART 推翻」。
  **照計畫書分工寫，就會產生這種跨頁矛盾，而且沒有任何稽核抓得到。**
- **搬過去還有一個比「歸類」更強的理由**：那個對話框的第二段依賴 `Layers`，
  而 `Layers` 正是 PART 3 第 5 節教的、就在第 6 節前面一節。
  **放在 PART 3，前置知識剛講完；放在 PART 6，我得再解釋一次**——
  我寫 PART 6 時真的多寫了一個框去補那個連結，那就是「位置不對」的徵兆。

**改法：**
- PART 3 第 6 節重寫成**三種下載並排**（清單／一個東西／一批東西），各一個小標。
  「一顆一顆」那句改成「影像要用底下那兩種拿」。
- 作業單多一步：第 4 步剛好把一列勾進 `Layers`，**第 5 步就打包那一個帶走**——
  而且拿到的正是 PART 5 第 4 節拿去跟原始資料比對的那份骨架。**四步變五步，銜接是白送的。**
- PART 6 第 2 節從「介紹打包下載」改成「**滑鼠這一層，以及它的天花板**」：
  回顧前五個部分做過的事，再列出這一層做不到的三件（批次、算不重複的個數、接到下一步）
  ——**那三件正好就是後面兩層存在的理由**。那一節因此從一個功能介紹變成整頁的轉折點。
- PART 6 的作業單也跟著重排：原本第 1 步是打包下載（已移走），現在三步全在這一 PART 的
  主題上——程式問一次（510）、加一個參數再問一次（481）、接上 MCP 並追問它的參數。

**可以固定下來的一條**：**每寫完一個 PART，回頭問一次「這一頁有沒有哪句話，
會被還沒寫的那幾頁推翻」。** 計畫書的分工是寫作順序，不是知識的邊界。

---

## 2026-09-14　PART 7（新專題：用 VFB 的資料重算一篇論文）

### 58. 選題與靶：硬條件是「論文的數字拿不拿得到」，不是「資料在不在」

使用者要加一個部分：**純粹用 VFB 的資料，重算一篇重要論文的一部分結果**，
形式照 LPU 的 PART 3／4／5（交代 AI Agent → 實跑 → 讀程式，程式可下載）。
討論後決定**新開 PART 7／8／9**（PART 6 是工具，是它的前置），論文選 **Nern et al., 2025, Nature**
〈Connectome-driven neural inventory of a complete visual system〉。

- **我一開始把硬條件想錯了。** 原本列的是「資料在不在 VFB 上」；
  實際卡關的是**論文有沒有公布逐項的數字**——沒有那個，「重算對不對」無從判斷。
  這篇過關是因為補充表逐型列著顆數：778 列 → **732 個型、52,827 顆**，
  跟正文「732 cell types、around 53,000」完全吻合。
- **補充表的下載網址有坑**：PMC 那條路回 200 但只有 1.8 KB，是空殼；
  要從出版社的靜態內容站（`static-content.springer.com/esm/…`）抓才拿得到 688 KB 的 zip。
  已寫進 `scripts/paper_target.py` 的檔頭。
- **我在推薦順序上判斷錯過一次。** 原本說候選 B（細胞型普查）「第一步就是定義爭議，
  比較適合當第二輪」——使用者問「第二輪是指什麼」，追下去發現兩個問題：
  「第二輪」是我自己假設出來、他沒說過的後續；而**那個理由本身也是錯的**，
  因為 LPU 的 PART 3 整個存在理由就是「論文沒交代的十個決定」——
  **這個形式本來就是為了處理定義爭議而設計的。**
  量過之後也證明沒那麼嚴重：844 個類別 ＝ 23 個泛稱 ＋ **821 個末端類別**（涵蓋 98.4% 的神經元），
  一條規則就分乾淨。

### 59. 比對的鍵選錯會差 22 個百分點——而這件事成了指令 5

兩邊的名字寫法不同：VFB 用全名（`transmedullary neuron Tm3`），論文用縮寫（`Tm3`）。
**用什麼當鍵，結果差很多：**

| 鍵 | 對得上 |
|---|---|
| VFB 的**類別標籤**取最後一個詞 | 547 / 732　**74.7%** |
| VFB 的**神經元標籤**取括號前那一段（`Dm20_R`） | **753 / 778　96.8%** |

而且以 instance 名比對時，**對上的那些裡有 732 筆顆數完全相同**。

- **這個發現直接變成指令 5 的內容**：要求 agent **做兩種、都回報命中率、再選一個**，
  而不是「選一個好的」。因為只做一種，那個命中率就沒有比較對象，
  **你無從判斷那是資料真的對不上，還是鍵挑錯了**。
- 注意一個巧合：「顆數相同的有 732 筆」跟「論文有 732 個型」**是兩個不同的量**，
  數字剛好一樣。頁面上不要把它們寫在一起，會被讀成同一件事。

### 60. 論文的數字也要有一份存下來的檔案

寫 PART 7 時 `number_audit` 叫出 `53,000`、`732` 對不回 `out/`——**它是對的**。
這門課的硬條件是「頁面上每個數字都要有一份存下來的輸出可以對回去」，
而論文的數字原本只寫在頁面上。已新增 `scripts/paper_target.py` →
`out/nern2025_target.json`，存論文的識別資訊、授權狀態、正文引句與補充表算出來的靶。

同一輪還修了三件小事：
- **DOI 不是資料的數字**（`doi:10.1038/…` 被切成 `10.1038`），已加進 `number_audit` 的 SKIP。
- **指令裡「超過 30 筆就給前 30 筆」的 30 是我自己訂的**，已列 EXEMPT 並寫理由。
- **我在正文寫了一個「匹配率 96%」當示意**——那是隨手編的數字，稽核抓到了，已改成不帶數字的說法。
  **示意用的數字也是數字**，這條之前沒有明講過。

`field_audit` 也叫出 `has_reference`（頁面指名了這個知識庫關係而探針沒存）——
第三次同一型。補存時踩到一個新坑：**存成中文句子沒有用**，因為對照集會跳過含中文的字串
（第 47 條那條規則），要另外存成一個純英文的欄位。

---

## 2026-09-14　PART 8／9（實跑那十條，並把程式整理成帶得走的東西）

### 61. 「預先知道答案」這件事必須寫在頁面上，不是寫在這裡

PART 7 那十條指令之所以寫得出來，是因為**寫它們之前已經跑過一次可行性查證**。
所以 PART 8 不是一次盲測——論文那一側的靶、VFB 那一側的顆數、兩種鍵的命中率、
逐列比對的 753／732／21，**全部是預先知道的**。

一開始我打算直接把數字寫上去，用「這一輪跑出來」的口氣。使用者的裁決是
「這樣處理可以，明講哪些是預先知道的」，於是 PART 8 的第 1 節整節就是那張交代表，
而且加了一段寫明**這一頁能證明什麼、不能證明什麼**：能證明流程跑得起來、
重跑會得到同一組數字；**不能證明一個沒看過答案的 Agent 照這十條做也會得到同一組**。

**這一條是通則，不只這一頁。**凡是「我們重算了一次」這種句子，都要先答一個問題：
**算的人事先知道答案嗎？**知道的話，那不叫驗算，叫重現。

### 62. 指令 8 真的抓到東西了——而我第一次寫的解釋是錯的

換第二條路問「資料集底下有幾顆神經元」，三條路給了兩個答案：
知識庫 53,402、MCP 53,402、**REST 的 `DatasetImages` 報 60,002**，而且在五萬列被截斷
（正是指令 3 預先警告過的坑）。

我第一版在 `cross_check.py` 的 `note` 裡寫的解釋是「同一顆有多張影像、
或掛在不只一個模板座標框底下」。**寫完才去驗，驗出來是錯的**：
實際抓一批列回來看重複的 id，重複的兩列**模板相同、縮圖相同，只有 `type` 欄不同**——
`Dm15_R` 既是 `Dm15` 也是 `adult glutamatergic neuron`，所以出現兩次。
真正的機制是**每一個（個體, 類別）配對給一列**。

再用 Cypher 驗：配對數 59,739 ＝ 53,402 ＋ 5,357 ＋ 2×490（掛 1／2／3 個類別的顆數）。
又多算一次（個體, 類別, 模板）三元組，**也是 59,739**，所以模板不是第二個來源。
**但 REST 報的是 60,002，還差 263 列，查不出來。**

那 263 就照指令 7 最後那句話辦：寫成一筆明講的未解項，留在 `cross_check.json` 的
`note` 欄與 PART 8 第 5 節，**不編解釋**。

**教訓（同一型第三次）**：先寫出一個聽起來合理的機制、再去驗，
跟先驗、再寫，出來的東西完全不同。第 36、43 條是同一件事。

### 63. 參數表裡有兩個參數是假的——寫 PART 9 時才發現

PART 9 第 2 節要講「第一個要看的地方是自己決定的參數」。寫到一半去對程式，發現
`recompute.py` 的 `PARAMS` 有四個，其中：

- `page_size`（「每頁取幾列」）——**從頭到尾沒有任何一行讀它**。這支程式走知識庫，
  一次就取完，根本沒有翻頁。
- `match_key`——註解寫得很完整，但比對那一段**寫死了用 instance**，改它不會有任何效果。

兩個都改掉了：`page_size` 刪除，`match_key` 補成真的會生效（換鍵的時候
**論文那一側也要跟著從 instance 欄換到 cell type 欄**，不然比的是兩種東西）。
補好之後實跑一次驗證它真的有效：`class` 那一組得到 547／535／12，
跟 `instance` 的 753／732／21 完全不同，數字存進 `run_meta.json` 的
`match_key_class_variant`，成了 PART 9 最有力的一張表。

**這條的價值不在「我改好了」，在於它自己變成了教材**：
PART 9 第 6 節那五個問題的第 1 條，現在是「找到參數表之後，逐一問這個真的有被用到嗎」，
而例子就是我們自己這一份。**一個不生效的參數比沒有這個參數更糟——
它讓人以為自己控制得住某件事。**

### 64. 「一半是註解」是我隨口寫的，實際量出來是兩成

PART 9 第 1 節原本寫「三支加起來 526 行，其中有一半是註解」。
`number_audit` 沒抓到（526 是合計，列進 EXEMPT 就過了），但那句話裡的「一半」
根本沒量過。用 `tokenize` 數 COMMENT 與 docstring 佔掉的行：**104 行，兩成**。

**這一型跟第 46 條（示意用的數字也是數字）是同一個**：
凡是帶比例的形容（「一半」「大部分」「幾乎全部」），都要當成數字處理——
要嘛量出來存進 `out/`，要嘛不要寫。

### 65. 稽核的掃描範圍要跟著新資料夾走，這件事很容易忘

PART 8／9 的數字來自 `recompute/out/`，而 `number_audit` 與 `field_audit`
原本只掃 `out/*.json` 與 `out/ui/*.json`。第一次跑 PART 8 的稽核，
47,555／5,357／490 三個數字「對不回」——不是數字錯，是**稽核看不到那個資料夾**。

做法：把 `recompute/out/` 的 JSON 複製一份到 `out/recompute/`，
兩支稽核的 glob 各加一行。**副本而不是搬過去**，因為那一包要能獨立解壓縮就跑，
`out/` 是它自己的產出目錄。代價是要記得同步——已寫進 repo 的 `README.md`。

另外 `quote_audit` 的來源清單也加了一條：論文全文走 Europe PMC 的 `fullTextXML`
（**PMC 的網頁版會擋自動存取、回 reCAPTCHA 頁**，那條路不能用）。
PART 8 引的三條論文原句因此都逐字驗得過。

### 66. 兩條指令要改，而兩條都是同一個毛病

實跑完，十條裡有兩條不夠用：

- **指令 1**：要求「說明你是從哪個網址拿到的」，但沒要求**看一眼檔案有多大**。
  PMC 那條路回 HTTP 200、內容型態正常、檔案 1.8 KB——是個空殼。
  應該加「回報檔案大小，太小就停下來」。
- **指令 7**：要求「挑三筆去查清楚」，但沒說**三筆要怎麼挑**。
  B 類有 184 個名字，抽三個就下判斷，證據太薄。
  應該改成「按前綴分組，每一組抽一個」。

兩條都不是 AI 做錯，是**檢查訂得不夠嚴**。這已經寫進 PART 8 第 6 節那張表，
**沒有回頭去改 PART 7** —— 因為 PART 7 是「指令長什麼樣」，
PART 8 是「跑完之後發現要改哪裡」，**把後者的結論倒回前者，那一課就不見了**。

---

---

## 2026-09-14　補 PART 8：778 列為什麼只有 732 個型

### 67. 頁面上並排的兩個數字，沒解釋就是一個坑

PART 8 第 2 節本來就同時寫著「778 列」與「732 個型」，而且還特地指出
「顆數相同的 732 列」跟「論文的 732 個型」是兩個碰巧相等的東西。
**但從頭到尾沒有解釋 778 跟 732 差在哪裡。**使用者直接問了這個。

答案很乾淨：**一列是一個（型, 側別）實例，不是一個型。**
778 − 732 ＝ 46，而剛好有 46 個型佔兩列，**46 個全部是 `_L`／`_R` 成對**。
686 × 1 ＋ 46 × 2 ＝ 778。

再往下看側別分佈，這件事才真的講得通：這份連線體重建的是**右側**視葉。
- 只有 `_R` 的 635 個型：VPN 319、ONIN 146、ONCN 94——視葉自己的神經元。
- 兩側都有的 46 個：VPN 33、VCN 6——連中腦的，左右兩顆都伸進右視葉。
- 只有 `_L` 的 51 個：**VCN 44**——中腦送訊號進視葉的那一群。

**`ONIN` 一個都沒有兩側**，剛好合它的定義（突觸不出視葉）。

**旁證**：論文正文另外寫了 *We identified 104 VCN types across about 270 cells*，
而補充表的 VCN 那一群算出來是 **104 個型、110 列、270 顆**——
**論文報型、表列列，兩個單位在同一句話裡同時出現，一個字都不差。**
這一條比我自己的推理有力得多，已經寫進頁面。

**通則**：頁面上並排出現、而且不相等的兩個數字，讀者一定會問為什麼。
**「都有出處」不等於「講清楚了」**——出處行證明數字沒編，不證明關係說明了。
`number_audit` 查的是前者，後者只有人看得出來。

**補這一段的時候，我又犯了一次同一個錯**（使用者當場指出）：
我在句子裡寫「681 列是 `_R`」，緊接著放一張「型數」的表（635／46／51），
**兩組數字並排、單位不同、又沒寫換算**，看起來就是 681 跟 635 打架。
其實完全對得上——`_R` 的列數 ＝ 只有 `_R` 的型 635 ＋ 兩側都有的型 46 ＝ 681；
`_L` ＝ 46 ＋ 51 ＝ 97。**這正是整段在講的那件事，而我自己在講它的時候又踩了一次。**

做法：表格加一欄「各佔幾列」與一列合計（732 個型／778 列），
換算存進 `target.json` 的 `sides.rows_contributed_by_side`，讓它可以被重跑驗證。
**教訓寫具體一點：一段文字裡出現兩種計數單位，就要有一行算式把它們接起來——
不是加註解說「單位不同」，是把加法寫出來。**

### 68. 又一次：填進去的數字沒量就寫

補完之後，程式從 267 行長到 302 行，於是頁面上的合計要跟著改。
我把「其中 118 行是註解」直接填進去——**118 是憑印象寫的，實際量出來是 109**。
更糟的是 **`number_audit` 放行了**：118 剛好出現在某支探針輸出裡，
而這一道查的是「這個數字有沒有在 out/ 裡出現過」，不是「出現在對的地方」。

**這正是那支程式自己在檔頭聲明過的天花板**（「把 46 改成 47 它抓不到」），
第一次真的被踩到。`--where` 印出來就看得見：109 對到 `run_meta.json`，
118 對到的是別的檔案。**改完數字要用 `--where` 掃一眼**這條規矩不是客套話。

跟第 64 條同一型（「一半是註解」沒量過），**兩次都出在同一個句子上**。

---

---

## 2026-09-14　把 A 類那 18 個追到底：換一個鍵，兩類同時縮小

### 69. 名字對不上不代表東西不在——該問的是「有沒有兩邊共用的編號」

使用者要我把「只在論文那 18 個型」再查一次。先用名字查（前綴、CONTAINS、
同義詞、整個知識庫），結論是「這些名字在 VFB 存在，但只出現在 `Berg2025`
與 `Xu2020`，不在 `Nern2024`」——**看起來像個答案，其實是死路**。

轉折點是想到論文補充表有一欄 `bodyId in figures`，而 VFB 的標籤裡也帶著
同一個編號（`Dm15_R (JRC_OpticLobe:65558)`）。**用編號查，25 列全部有下落**：

- ① 一對一改名，顆數相同：**10 列**（`HST_R`→`HS4_R`、`LAL047_R`→`LoVC8_R`、
  `LC35a_R`→`LoVP15_R`、`LT85_R`→`LoVP87_R`、`V1_L`→`LPT58_L`、`mALD1_L`→`LoVC10_L`…）
- ② 側別標到另一邊：**1 列**（`DNc01_L` 在 VFB 併進 `DNc01_R`，所以 VFB 那列是 2 顆）
  ——**這同時解掉那 21 筆顆數差異裡的一筆**。
- ③ 論文拆、VFB 併：**7 列**（`Li11a+b`→`Li11_R` 2+2=4、`LoVP90a/b/c`→`LoVP90_R` 1+1+1=3、
  `VST1+VST2`→`VT_R` 3+4=**7**）。**三組加總都跟 VFB 那一列的顆數完全相等。**
- ④ VFB 真的沒收：**7 列 11 顆**（`AN09A005` 左右、`AN27X013` 左右，
  以及三個帶 `TBD`（待定）的）。這 7 個的編號在 `JRC_OpticLobe` 命名空間裡
  **完全不存在**，而名字只出現在 `Berg2025`。**為什麼沒收，仍然不知道。**

**連帶效果**：那 184 個「只在 VFB」的名字裡有 **12 個**其實就是上面改名後的名字，
扣掉之後是 **172**。**換一個鍵，A 與 B 兩類同時縮小。**

**通則**：`指令 5` 要求「至少兩種鍵、都回報數字」——方向對了，但我自己
**兩種都選了名字**。要補的一句是「**有沒有一個兩邊共用的編號**」。
名字是人取的、會改；編號是同一份重建的身分證。

### 70. 用字串猜身分，猜錯了一個

上一版靠「去掉名字最後那個小寫字母」推論出「`LC39`、`Li11`、`LoVP90`
三個型被論文拆成 a／b／c，VFB 還合著」，並寫進了頁面。

**編號查出來不是這樣**：`LC39a_R` 確實對到 `LC39_R`，但 **`LC39b_R` 對到的是
`LoVP51_R`**——一個完全不同的名字。所以 `LC39` 根本不是「拆分」那一類，
是①的一對一改名。真正「論文拆、VFB 併」的只有 `Li11` 與 `LoVP90`
（外加不屬於 a/b/c 形式的 `VST1+VST2`→`VT`）。

**教訓：字串猜得出形狀，猜不出身分。**那條 `^(.*?)([a-z])(_[LR])$` 的規則
在 7 個裡對了 6 個，而**錯的那一個不會自己舉手**——它照樣被歸進「已解釋」。
這跟第 62 條（先編機制再驗）是同一型：**先有一個合理的故事，就不會再去找證據。**

### 71. 兩個數字並排卻沒解釋——一天之內被問了兩次

第 67 條記的是「778 列 vs 732 型」。同一天使用者又問了
「844 個類別，其中泛稱 23 個、末端類別 821 個」是什麼意思。
**兩次都是同一個毛病**：表格裡並排兩個數字，出處行齊全，但**關係沒寫**。

補進頁面的內容（`out/recompute/vfb.json` 的 `generic_classes` 存了完整清單）：
- **末端類別**＝在這 844 個裡底下沒有別的類別，那才是「一個細胞型」。
- **泛稱**有兩種味道：**分類學上位**（`adult visual projection neuron` 底下 367 個）
  與**橫切的屬性**（`adult cholinergic neuron` 底下 277 個——那是傳導物質，不是形態型）。
- **為什麼非排掉不可**：全當成型的話顆數加總 59,739，比總數多 6,337；
  光 `adult cholinergic neuron` 就直接掛了 4,069 顆。
- **最該看的是邊界案例**：`LC10`（底下 5 個）、`Dm3`（2 個）、`Cm11`（1 個）
  看起來已經是型了卻被判成泛稱——**「什麼算一個型」在這裡沒有客觀答案。**
- **也要講明我們規則自己的邊界**：只看這 844 個之內。某類別在整個本體論裡
  還有下位、但那些下位在這套資料裡沒有神經元，它在這裡仍算末端。

**通則（第 67 條的加強版）**：教材裡任何一處出現「N 個，其中 A 個…、B 個…」，
**都要有一段講 A 與 B 的判準是什麼、以及判錯會怎樣**。
出處行證明數字沒編，`number_audit` 也只查得到這件事——**關係說明了沒有，只有人看得出來。**

---

---

## 2026-09-14　那 7 列為什麼沒收：整個第 4 節收斂成「差一個版本」

### 72. 答案一直寫在資料集的描述欄裡

使用者要我查「那 7 列為什麼沒收」。第一件事不是去問 VFB，是**去讀論文的方法段**——
結果它有一整節就叫〈Versions of the dataset〉。

**VFB 收的是 `optic-lobe:v1.0.1`，論文補充表是 `v1.1`。**
這不是推論：**VFB 自己的資料集描述欄就寫著**
「Optic Lobe connectome neurons from optic_lobe:v1.0.1」，而論文的資料可用性
指向 `?dataset=optic-lobe:v1.1`。**我從 PART 7 起就把 v1.0.1 印在頁面上，
卻一直沒把它跟論文的版本對一下。**

**這一條的教訓很難聽但很值錢**：前面繞了兩輪（名字查、bodyId 查）才走到這裡，
而**第一輪就該做的是「兩邊各是哪一版」**。這正是 `PART 4 第 4 節`自己教的事
（同一套資料有不同版本），**我寫了那一節，然後在下一個專題裡忘了用。**

### 73. 用論文自己的數字驗自己的推論：5 → 11，一個字不差

光說「差一個版本」還是個故事。論文那一節剛好有一句可以直接對答案的話：
*We also revised some cell-type definitions (5 types were split into a total of
11 new types and 2 types were merged into a single type).*

於是把**全部 778 列**都用 `bodyId` 對到 VFB 那一側，看能不能自己數出來：

| v1.0.1 | → v1.1 |
|---|---|
| `VT_R` | `VST1_R`、`VST2_R` |
| `Li11_R` | `Li11a_R`、`Li11b_R` |
| `LoVP90_R` | `LoVP90a/b/c_R` |
| `LoVP70_R` | `LoVP51_R`、`LoVP70_R` |
| `DNc01_R` | `DNc01_L`、`DNc01_R` |

**5 個型 → 11 個新型。跟論文寫的完全一樣，而算的時候沒有看那句話。**

**這才是「第二條路」該長的樣子**（指令 8）：不是換個 API 問同一個數字，
是**用完全不同的證據把同一個結論再推一次**。前面那次（三條路問神經元總數）
比的是同一份資料的三個介面；這一次比的是兩個獨立來源。

### 74. 一個版本差，解掉了三件原本各自掛著的事

- **7 列沒收**：v1.1「把原本散落、沒接起來的片段併進神經元」，完整度小幅提升。
  那 7 列在 v1.0.1 還不成形，所以沒有編號。**三個旁證**：
  ① 7 列**全部落在 `other` 那一群**（778 列裡對不到的也就這 7 列）——
  `other` 是「中腦與視葉都有突觸、但不算 VPN 也不算 VCN」，身體大半在範圍外；
  ② 三個 `_TBD` 就是論文自己說的「三個佔位名」；
  ③ 那五個名字在 VFB 只出現在 `Berg2025`（Male CNS 全腦），
  而視葉這套的描述寫著 *traced from part of the Male CNS EM volume*——同一份影像，
  全 CNS 那版裡上行神經元是完整的。
- **`R7p` ＋202／`R8p` ＋199**：論文版本註記明寫 v1.1
  *adjusted the typing of some individual cells (nearly all of these are R7 and R8
  photoreceptors*。**第 4 條那個 `R7_unclear` 的猜測就此作廢**——
  它沒有出處，版本註記有。
- **25 列改名／拆型**：全部是 v1.0.1 → v1.1 的改動，論文列在同一節。

**通則**：對不上的東西先別歸因到「資料有問題」。
**先問「兩邊是同一版嗎」，再問「作者有沒有自己寫下版本之間改了什麼」。**

### 75. 明講方法的天花板，免得下一個人以為驗過了

論文那句話有後半段：「**2 個型被併成 1 個**」。這個我們**驗不出來**——
補充表的 `bodyId` 欄只給「圖裡用的那一顆」，所以做的是型層級對照；
被併掉的那一個型在 v1.1 沒有自己的列，**也就沒有代表編號可查**。

已經寫進 `version_gap.caveat` 與頁面。**這是方法的天花板，不是資料的問題**——
跟第 68 條（`number_audit` 的天花板）同一個性質：
**能驗的寫清楚驗了什麼，不能驗的寫清楚為什麼不能。**

---

---

## 2026-09-14　一句自相矛盾的話

### 76. 「不是巧合……它們碰巧相等」——同一句話裡答了兩個問題

PART 8 第 2 節原本寫著：

> 兩個 732 是巧合嗎？**不是**，但也不是同一件事：前者是論文的分型數，
> 後者是逐列比對後顆數相同的列數。**它們碰巧相等**……

使用者直接指出：又說不是巧合、又說碰巧相等。**他是對的，而且更糟的是：
正確答案就是「是巧合」。**我把「是不是巧合」跟「是不是同一件事」兩個問題
混在一句話裡答，結果兩邊都答歪了。

拆開來看，巧合得非常乾淨——**兩個 732 都等於 778 減 46，而那兩個 46 毫無關係**：

| | 算式 | 那個 46 是什麼 |
|---|---|---|
| 論文的分型數 | 778 − 46 = 732 | **46 個型各佔兩列**（`_L`／`_R`） |
| 顆數相同的列數 | 778 − (21 + 25) = 732 | **21** 列顆數不同 ＋ **25** 列 VFB 沒有 |

一個講左右側，一個講比對結果，之間沒有任何因果。

**為什麼會寫成那樣**：我當時想強調的是「別把兩個 732 混為一談」，
而「是巧合嗎？不是」那個開頭是**修辭上的反問句順手寫下去的**——
反問句一旦跟真正的答案相反，整句就壞了。

**通則**：教材裡不要用「A 嗎？不是」這種反問開頭去帶一個**其實是「是」**的答案。
**要防的是讀者誤會，不是讓句子讀起來有轉折。**
這一條跟第 67、71 條（並排的數字沒解釋）是同一族——
**都是「我知道自己在說什麼，所以沒發現讀者不知道」。**

---

---

## 2026-09-14　兩個分母不同的百分比，不該並排寫

### 77. 96.8% 對 74.7%——並排看起來可比，其實不可比

使用者問：「這裡兩個比對的分子和分母各是什麼？為什麼兩個比對的分母不同？」

拆開來是這樣：

| 鍵 | 分子 | 分母 |
|---|---|---|
| 個體標籤（`Dm20_R`） | 753 | **778 列**（論文表的 instance，帶側別） |
| 類別標籤（`Dm15`） | 547 | **732 個型**（論文表的 cell type，不帶側別） |

**分母不同有正當理由**：兩種鍵的粒度不同。VFB 的個體標籤帶側別，
只能跟論文的 `instance` 欄比；類別標籤不帶側別，只能跟 `cell type` 欄比。
**換鍵的時候兩邊都得換**——這件事我還寫進了 PART 9 第 3 節。

**但「分母不同有理由」不等於「可以並排寫成兩個百分比」。**
我把 96.8% 與 74.7% 擺在同一格，讀者自然會相減。**那個減法是無效的。**

補算了同分母的版本（兩種鍵都降到型這一層，分母統一用 732）：
**個體標籤 711/732 ＝ 97.1%，類別標籤 547/732 ＝ 74.7%，差 22.4 個百分點。**
結論不受影響，而且更乾淨。程式現在**每一行比率都把分母印出來**，
並多印一行同分母的對照。

**通則**：只要一頁上出現兩個以上的百分比，就要回答兩個問題——
**各自的分母是什麼、它們能不能相減。**能相減就寫在一起，
不能就先換算到同一個分母，或者乾脆不要並排。
**這跟第 67、71、76 條是同一族**：我心裡知道那兩個分母不同，所以沒發現讀者不知道。
**這一族已經四條了，該當成一條硬規矩而不是教訓。**

### 78. 順手踩到第 68 條那個坑的第二次

同分母那組數字裡有一個 **711**。而 `number_audit` 的 `EXEMPT` 裡剛好有一筆
「711 ＝ 452 ＋ 168 ＋ 91（三支程式的行數合計）」——**程式的總行數也是 711**。

`EXEMPT` 比 `out/` 先查，所以稽核直接放行，`--where` 印出來是
「711 EXEMPT」，**完全看不出頁面上那個 711 其實指的是別的東西**。

修法：行數合計現在已經存進 `run_meta.json` 的 `code_lines.total_py`，
那筆 `EXEMPT` 根本不需要，刪掉。刪掉之後 `--where` 印的是
「711 直接 compare.json、run_meta.json」——**兩個來源都看得到，人才判斷得了**。

**教訓**：`EXEMPT` 是一份「這個數字查不到但沒問題」的交代，
**每加一筆就等於在稽核上開一個洞**。凡是後來變得查得到的，要記得回去把洞補上。

---

---

## 2026-09-14　一句指錯欄位的導言

### 79. 「最右邊那兩欄／中間兩欄」——那張表只有四欄

PART 8 第 2 節那張側別表的導言寫著：

> 把 732 個型按側別攤開——**最右邊那兩欄是型，中間兩欄是列**，兩者的換算就寫在表裡

使用者：「這在寫什麼？總共就四欄啊？」——**表確實只有四欄**
（在表裡出現在／型數／各佔幾列／主要是哪一群），而正確的說法是
**第二欄是型、第三欄是列**。改掉了。

**怎麼寫出來的**：這張表本來只有三欄，第 67 條那一輪為了把「型」與「列」
接起來才加上「各佔幾列」。**我加了欄位，卻沒回頭改導言**——
導言那句是照舊版的欄位數寫的，加完欄位之後它就不對了。

**這是第 67 條那一族的新變種**：前幾條是「數字並排沒解釋」，
這一條是「解釋了，但指錯地方」。**兩者都讓讀者停在同一個位置。**

**具體規矩**：改動表格的欄位之後，**回頭讀一次它上面那一句**。
凡是「最左／最右／中間／第 N 欄／那兩欄」這種指位詞，都跟欄位數綁著。

**順手把全站掃了一次**（`grep` 所有指位詞），其餘幾處都指得準：
- `part5-nblast.html` 說 NBLAST 結果表「最右邊是 `Score ▼`、左邊第二欄是 `Type`」
  ——**拿畫面探針驗過**：`out/ui/nblast_results.json` 的 `columns_on_screen`
  是 `Name, Type, Gross_Type, Template_Space, Imaging_Technique, Images, Score ▼`，兩句都對。
- `part6-api.html`、`part1-templates.html`、`part7-agent-prompts.html` 各一處，都對。

**值得記的是**：指位詞這一類，`field_audit` 與 `ui_claim_audit` **都查不到**
（它查欄位名對不對，不查「第幾欄」對不對）。這是第三道天花板，
目前只能靠改表時人工回頭看。

---

---

## 2026-09-14　刪掉一段「查完就沒再用到」的旁證

### 80. 查證做得漂亮，不等於它該留在頁面上

PART 8 第 2 節有一段 VCN 的旁證：論文正文寫著
*We identified 104 VCN types across about 270 cells*，而補充表的 VCN 那一群
算出來是 **104 個型、110 列、270 顆**——論文報型、表列列，一個字都不差。

使用者問：「這一段內容之後會用到嗎？不會的話就刪掉。」
`grep` 了全站：**104、110、270 在別處一次都沒出現。** 刪了。

**它其實是個好旁證**——那一節其餘的論證都是我們自己的算術
（778 ＝ 686 ＋ 46×2），只有這一段是**外部證據**，證明我們對「型 vs 列」
的讀法跟論文自己的用法一致。**但「好」不是留下來的理由，「後面會用到」才是。**

**我為什麼會寫進去**：查的時候很興奮——三個數字全中。
**那是查證者的成就感，不是讀者的需要。**這一頁已經用兩張表把型與列的換算
攤開了，再加一段等於把同一件事說第三次。

**連帶清掉的**：`recompute.py` 裡那段 `vcn_check`（連同註解 11 行）
與 `paper_quotes.json` 裡那條引文，**都只為那一段而存在**。
留著就成了 PART 9 第 6 節第 1 條自己在罵的東西——**沒人用到的程式**。

**通則**：查證的產物分兩種——**支撐頁面上某個句子的**，與**只滿足查證者的**。
第二種收進 `_notes` 就好。**寫完一段就問一次：後面哪一句會回頭指它？
指不到，就是這一種。**

---

---

## 2026-09-14　依同一把尺再掃一次 PART 8

### 81. 又找到一段死路——而它裡面還有一個站不住的宣稱

使用者要我用第 80 條那把尺（「後面會用到嗎」）再掃一次 PART 8。
作法：把全頁的數字抽出來，看哪些只出現在單一節、而且沒有別的句子回頭指它；
再把可疑的幾節整段讀出來判斷。

找到的是第 3 節那個 `<div class="gap">`：**〈再往下挖一層：那 200 顆為什麼全部落在「pale」那一列〉**。
它引了 Methods 的分型步驟、掃了補充表找 `unclear`、查了 VFB 的 `FBbt_02000006`，
**最後的結論是「我們一度以為……那只是個猜測，見第 4c 節」**——
整段的產物是一個已經作廢的假設。刪了。

**但它上面那一段更要緊，而且我差點只把死路刪掉就收工**：

> 論文明說 R7／R8 被少算了，估計是 **459** 顆。我們算出來的差是 **402** 顆，
> **落在同一個量級**。

**這是拿兩個不同的量在相比。**論文的 459 是「相對於真實果蠅，這份重建少了
多少光受器」；我們的 402 是「v1.0.1 與 v1.1 兩版之間的分型差」。
**兩者大小相近純屬巧合，而我把它寫成了佐證。**改成直接指向第 4c 節那個
有出處的版本解釋，459 與 2,777 那條引文一併移除（只為那段而存在）。

**教訓**：掃死路的時候順手重讀一次，比只刪段落值錢——
**死路旁邊常常就是當初為了把死路講圓而寫下的過度宣稱。**

### 82. 連帶清掉三處「上一版我們寫錯了」

刪完之後有兩處在指已經不存在的東西（`R7_unclear` 全頁不再出現），
順著查下去發現第 7 節那個「還沒解的」框**已經變成我自己的修正史**：
兩條加了刪除線的舊條目、各配一句「已解／有更好的答案了」。

**那正是 `specific_topics/CLAUDE.md` 明令教學頁不該留的東西**
（學員看結論，修正紀錄收在不部署的 `_notes/`）。重寫成四條乾淨的未解項。

同一把尺也用在第 4b 節那個「這一輪順手推翻了我們自己上一版的一個結論」上。
**那一段的教訓是真的**（`LC39b_R` 對到的其實是 `LoVP51_R`，字串猜得出形狀
猜不出身分），**但框架是編輯史。**改寫成「為什麼非得用編號不可——光看名字
會猜錯」，證據與結論全留，**只把「我們上一版錯了」這個主詞換掉**。

**分界線寫清楚，下次不用重想**：
- **留**：方法為什麼會失敗、一個天真作法會錯在哪裡——那是內容。
- **刪**：這一版跟上一版差在哪裡、我們原先寫了什麼——那是編輯史，收進 `_notes`。
- **例外**：第 1 節那張「哪些數字是預先知道的」表<b>要留</b>——
  它講的不是我改過什麼，是**這一輪的結論有多少是獨立產生的**，那是讀者判斷
  可信度的依據（而且是使用者當初指定要寫的）。

---

---

## 2026-09-14　「底下沒有別的類別」看不懂——而改寫時我又編了一個機制

### 83. 抽象的判準要配一張圖跟一個實例

使用者：「『末端類別（821 個）——在這 844 個裡底下沒有別的類別。』
底下沒有別的類別是什麼意思，看不懂，講清楚一點。」

原文用的是**本體論的行話**（上位／下位、`SUBCLASSOF`），
而讀者要的是「這句話在講什麼形狀」。改成一張樹：

```
adult neuron                              最粗
   └─ adult optic lobe intrinsic neuron
        └─ distal medullary amacrine neuron
             ├─ Dm15   ← 底下沒東西了，再也分不下去 → 末端類別
             └─ Dm3    ← 底下還站著更細的 → 泛稱
                  ├─ Dm3a   601 顆
                  ├─ Dm3b   571 顆
                  └─ Dm3c   0 顆（這套資料裡沒有）
```

**「底下沒有別的類別」→「再也分不下去的那一層」。**同一件事，後者不必先懂本體論。

**這是第 67、71、76、77 那一族的第五條**，但方向不同：
前四條是「兩個數字並排沒解釋」，這一條是**「判準只給了定義，沒給形狀」**。
**規矩補一句：凡是用「有沒有 X」當判準的地方，要配一個有 X 的例子跟一個沒有的例子。**

### 84. 改寫的時候我又編了一個機制，而且差點就發布了

改寫時我順手寫了一句聽起來很合理的話：

> 把統稱也當成一個型，那批細胞就會被數兩次（一次算在 `Dm3a`，一次算在 `Dm3`）。

**動手查了一次，是錯的**：`Dm3` 與 `Dm3a`／`Dm3b` 的重疊是 **0 顆**。
那 401 顆 `Dm3` 是**沒有再細分到 a／b 的那些**，不是重複。

**真正造成重複的是另一種泛稱**——橫切的屬性：
**4,063** 顆神經元既有自己的細胞型、又掛在「膽鹼性神經元」底下
（例：`Mi10_R` 同時是 <i>medulla intrinsic neuron Mi10</i> 與 <i>adult cholinergic neuron</i>）。
**那才是 59,739 比 53,402 多出 6,337 的來源。**

所以頁面改成一張兩列的表，把兩種泛稱分開講：**分類學上位不會重複數**
（但仍不能算型，因為那等於在同一份清單裡同時用粗細兩種分型標準）、
**橫切屬性會**。

**這是第 62、70 條的第三次**：先寫出一個合理的機制、再去驗，跟先驗再寫，
出來的東西不一樣。**而這一次特別值得記，因為我是在「修正一個說不清楚的地方」時犯的**——
改寫比初寫更容易掉進去，因為注意力全在「講清楚」，不在「這句是不是真的」。

### 85. 順手產的檔案要折回主程式，否則乾淨重跑會少東西

那張樹的實例我一開始是用臨時腳本查出來、直接寫進 `out/` 的。
**這樣一來，把 zip 解到空資料夾重跑 `recompute.py`，`class_tree_examples.json` 不會被重新產生**
——正是指令 10 第 4 項與 PART 9 第 6 節第 5 問在防的那件事，
也是第 65 條（稽核掃不到新資料夾）的近親。

折成 `class_tree_examples()` 進主程式，主流程多印一行
「Dm3 與 Dm3a／Dm3b 的重疊：0 顆」。重新從空資料夾驗過，四個檔全部一致。

**規矩**：凡是查出來、要放上頁面的東西，**產它的程式就得在交付的那一包裡**。
臨時腳本查完即丟，不可以留下產物。

---

---

## 2026-09-14　讀者照著我的圖數了一次，數出我沒講的那件事

### 86. 「那 844 個是怎麼來的」——整頁從來沒講過

使用者拿第 83 條那張樹去數：「就這棵樹來說，有 8 個類別，但只有 4 個末端類別
（假設 Dm15、Dm3a、Dm3b、Dm3c 底下都沒東西了）？」

**就那張圖而言，他數得完全正確。**但實際套上規則，答案是
**3 個泛稱、3 個末端、2 個根本不算**：

| 節點 | 直接掛幾顆 | 在 844？ | 判定 |
|---|---|---|---|
| `adult neuron` | 1 | 是 | 泛稱（底下 840） |
| `adult optic lobe intrinsic neuron` | 1 | 是 | 泛稱（底下 217） |
| `distal medullary amacrine neuron` | **0** | **否** | — |
| `Dm15` | 242 | 是 | 末端 |
| `Dm3` | **401** | 是 | 泛稱（底下 2） |
| `Dm3a` | 601 | 是 | 末端 |
| `Dm3b` | 571 | 是 | 末端 |
| `Dm3c` | **0** | **否** | — |

**差別來自一件我從頭到尾沒寫出來的事**：那 844 個
**不是本體論裡的所有節點，是「至少有一顆神經元被直接標成它」的類別**。
`distal medullary amacrine neuron` 是本體論上的中間層，這套資料裡沒有任何一顆
只被標到那一層，所以它連進場的資格都沒有；`Dm3c` 同理。

**這條沒寫出來，整個 844／23／821 就是一組沒有定義域的數字。**
而我前面已經為了「泛稱／末端」改寫過一次（第 83 條），
**改寫時只補了判準，沒補定義域**。

**還有一個順帶被讀者逼出來的點**：`Dm3` 自己直接掛著 **401 顆**。
泛稱身上也可以有神經元——那是「只被標到這麼細、沒再細分到 a／b」的那些。
**所以「泛稱」不等於「空的中間層」**，這件事原本的寫法完全看不出來。

做法：圖重畫，**每個節點都標上「直接幾顆／在不在 844／判定」**，
並在圖前面一句話講明 844 的定義域。逐節點的判定寫進
`class_tree_examples()` 的 `tree_nodes`，可以重跑驗證。

**通則（第 83 條的補充）**：畫一張圖來解釋一條判準時，
**圖上的每一個元素都要能套那條判準**。只標「符合的那幾個」，
讀者會自動假設其餘的都是另一類——而那個假設常常是錯的。

---

---

## 2026-09-15　讀者把樹上的數字讀成子樹總數，而我有兩層防護都沒擋住

### 87. 說明放在誤讀發生的位置之外，等於沒有放

使用者問：「`Dm3` 401 顆，然後分出來的兩類一個 601 顆一個 571 顆，是不是矛盾？」

**不矛盾**——那一欄是「直接標成它」的顆數，三者互斥（實際查過重疊是 0），
`Dm3` 那 401 顆是「只被標到這麼細、沒再細分到 a／b」的那些。

**但頁面已經寫了兩次，兩次都沒擋住**：圖**前面**那句括號寫著「數字是直接標成它的神經元顆數」，
圖**後面**還有一整段「另外注意 `Dm3` 那 401 顆」。為什麼沒用：

1. 括號在圖前面，讀者的眼睛直接落到圖上；
2. 圖裡每一列的單位就寫「顆」，而**在一棵樹裡，掛在父節點旁邊的數字預設就是讀成子樹總數**
   ——那是讀者的正確直覺，是我違反了它；
3. 上面兩層的 `1 顆`（`adult neuron` 顯然不只 1 顆）本該是個提示，但它太小，反而像雜訊。

**改法不是再加一段說明，是把兩個量同時放進誤讀發生的那一格**：
每一列變成「直接 ／ 這一叢共」兩欄，加一行欄頭；`cells_in_subtree` 補進探針
（`recompute.py` 的 `class_tree_examples()`，`SUBCLASSOF*0..8` 加 `count(DISTINCT n)`，
DISTINCT 是必要的——一顆神經元可以掛好幾個類別）；正文補上那條加法
401＋601＋571＝1,573。

**順帶多出一個教學點，而且是圖上本來就有的**：`distal medullary amacrine neuron`
直接掛 **0** 顆，但這一叢有 **4,131** 顆——**「沒有神經元直接標成它」不代表這一支是空的**。
原本那張圖只寫 0，讀者沒有理由不把它當成空的。

**通則（第 67、71、76、77、83、86 那一族的第七條）**：
**一個單位有歧義的數字，說明必須跟它同格。**寫在前一句或後一段都會被跳過——
這一次是兩邊都寫了，還是被跳過。**兩個量並存的時候，兩個都印出來，比解釋它們的差別更短也更可靠。**

---

## 2026-09-15　加一道「乾淨重跑」的稽核，當場抓到三件事

### 88. `run_meta.json` 沒有任何程式產生它——第 85 條那個病的第二例

改完樹狀圖，`recompute.py` 從 534 行長到 556 行，於是去更新 PART 9 的行數。
**結果 `run_meta.json` 裡的 534 怎麼重跑都不會變——因為沒有任何程式在產生它。**
它是臨時腳本寫出來的，裡面裝著行數、註解比例、換參數重跑的那一組數字、乾淨重跑的計時。

第 85 條把 `class_tree_examples.json` 折回主程式時，**沒有回頭查還有沒有別的孤兒檔**。
規矩訂了，只補了觸發它的那一個檔案。

做法：新增 `recompute/run_meta.py`，三件事都真的做——量行數、把 `match_key`
換成 `class` 重跑一次、把 zip 解到空資料夾重跑再逐位元組比對。

**它刻意不進交付的那一包**，產出直接寫 `../out/recompute/run_meta.json`：
**描述某個 zip 的檔案不能住在那個 zip 裡面**，否則它永遠差一版。
不給 `--zip` 時 `clean_run` 寫成 `null`，**不留上一次的舊值**——
乾淨重跑是對某一個 zip 的宣稱，沒驗就不該有數字。

### 89. 乾淨重跑第一次跑，就抓到第三個孤兒檔

`paper_quotes.json` 也沒有程式產生它。而頁面上那一行出處寫著
「論文的五條原句出自 `out/recompute/paper_quotes.json`（全文經 Europe PMC 取得，**逐字比對過**）」
——**那句「逐字比對過」沒有任何程式在維持**：論文換版、或我抄錯一個字，重跑都不會有人發現。

折成 `recompute.py` 的 `paper_quotes()`：抓 Europe PMC 的 `fullTextXML`、去標籤、
把空白壓成單一空格，六條逐條 `in` 比對，**對不上就 `SystemExit`**。
（六條不是五條——頁面那句寫少了一條，順手改掉。）

**這一條的價值在於「稽核的形狀」**：前兩個孤兒檔是我一個一個想起來的，
第三個是**程式自己報出來的**。`clean_run` 的 `not_regenerated` 欄就是這道稽核的產物——
凡是 `out/` 裡有、乾淨重跑卻沒產出來的，全部列出來。

### 90. `match_key="class"` 早就不能跑了，而 PART 9 正在教這件事

`run_meta.py` 第一次跑就當掉：`KeyError: '5-HTPMPV01'`。
原因是 `resolve_by_body_id()` 拿 `only_paper` 去索引 `target["by_instance"]`，
而 `match_key="class"` 時 `only_paper` 裝的是**型名**不是列名。

**這支函式是後來（用 bodyId 追 A 類那一輪）才加的，加的時候沒有回頭試另一個參數值。**
第 63 條實跑驗過 `class` 那一組（547／535／12）——**那是加 `resolve_by_body_id()` 之前的事**。

所以 PART 9 第 2 節那張「一個參數把結論從 732 改成 535」的表，數字是對的，
**但讀者照著改參數只會拿到一個 traceback**。
**這正是那一節自己在罵的東西的升級版**：上一輪的毛病是參數不生效，這一輪是參數會當掉。

改法：`by_body_id` 那一步加條件——只有 `match_key="instance"` 才做，
否則寫一行 `skipped` 說明為什麼（論文補充表的 bodyId 是逐列給的，
換成 class 這一側就沒有可對的編號）。**不硬做，明講跳過。**
改完重跑 `class` 那一組，547／535／12／118／185／192，**跟舊檔一字不差**。

### 91. `cross_check.json` 裡有時間戳，違反那一包自己寫的規矩

乾淨重跑比對到最後只剩這一個檔案不一樣，因為它有 `"when"` 欄。
而那一包的 `README.md` 第一句就寫著「**時間戳只寫在 `out/fetched_at.txt` 裡**，
其餘檔案不含時間。這樣重跑一次之後，`git diff` 看得出來的就只有真正變掉的數字」。

**規矩是 `recompute.py` 在守，`cross_check.py` 從來沒守過**，而**在加這道稽核之前沒有人比對過**，
所以看不見。改成另寫 `out/cross_check_at.txt`。

**教訓**：一條「所有檔案都要 X」的規矩，要有一個地方會因為違規而失敗，否則它只在寫它的人手上成立。

### 92. 我自己的計數器第一版多數了一倍，而我是用舊版程式驗出來的

`run_meta.py` 的註解行數第一版用 tokenize 抓「獨佔一行的字串」當 docstring，
量出 **310 行（38%）**，而頁面上寫的是 152 行（19%）。差一倍。

原因：`recompute.py` 滿是跨行的 Cypher 查詢字串——
```
        return cypher(
            "MATCH (:Class {short_form:'%s'})<-[:SUBCLASSOF]-(b:Class) "
            "RETURN b.short_form, b.label" % sf)
```
第二、三行的字串都「獨佔一行」，於是全被算成說明。**那些是程式，不是說明。**
改成註解用 `tokenize`、docstring 用 `ast`（只認 Module／ClassDef／FunctionDef 的第一句）。

**驗證方式值得記下來**：把 `git show HEAD:recompute/*.py` 倒進一個暫存目錄、
拿新的計數器去量，得到 **793／152／19%**——**跟頁面上寫的一模一樣**。
新工具要取代一個沒有程式的舊作法時，**先讓它在舊輸入上重現舊答案**，
再拿去量新輸入。否則數字一變，分不清是程式改了還是量法改了。

---

## 2026-09-15　PART 7 的 pager 掛著「（尚未發布）」，掛了一天

### 93. 佔位字樣不是連結，所以連結稽核看不到它

使用者問：「為什麼 PART 7 的頁面會出現『PART 8　真的讓它跑一遍（尚未發布）』？」

因為 PART 7 是在 PART 8 還不存在時寫的（commit `fe6bff1`），
**下一個 commit（`1016136`）就把 PART 8／9 補上了，但沒有人回頭改那兩個 pager。**
上下各一處，一共兩處。

**為什麼稽核抓不到**：第 2 道查的是「`href` 指的檔案在不在、`#frag` 對不對得到 `id`」，
而佔位是 `<span>`——**它沒有 href，所以那一道根本不會看它。**
「還沒做的東西」在頁面上一律長這樣：不是連結，因此不受任何連結稽核管轄。

做法：`page_audit.py` 補一道「佔位字樣」——掃 `尚未發布／還沒發布／即將推出／待補`，
把附近的 `PART N` 解析出來，**那一頁的檔案已經存在就是錯誤**；還不存在就只印一行提醒。

### 94. 新的一道稽核第一版沒有叫，是破壞測試抓到的

補完之後跑一次：通過。**但那是因為我已經把 PART 7 修好了。**
照這個專題的規矩把 PART 7 改回壞的樣子再跑一次——**它沒有叫。**

原因是視窗寫成 `[^\s]{0,30}`，而標題裡 `PART 8` 與 `真的讓它跑一遍` 之間
是**全形空格 U+3000**，Python 的 `\s` 吃得下它。於是抓到的片段從「真的」才開始，
裡面沒有 `PART 8`，程式走進「指的那一頁還沒有，先留著」那個不算錯的分支。
**一道只會印溫和提醒、從不算錯的稽核，跟沒有那道稽核是同一件事。**

改成 `[^\n]{0,40}`（留在同一行、但允許空白），並把編號取**最靠近那句話的那一個**。
再跑：壞的狀態下兩處都叫、`bad=2`；修好之後通過。

**這一條跟第 46 條（`ui_claim_audit` 放寬規則要破壞測試）是同一件事的另一半**：
**放寬規則要破壞測試，新增規則也要**——差別只在前者怕它變得太鬆，後者怕它從來沒緊過。

---

## 現在還沒解的（2026-09-14，這一節取代前面各節的待辦快照）

**真的還沒解的有八條：**

1. ~~FANC 到底在不在連線資料集清單裡~~ ——**已解**（第 49 條）：它有站台也有 2,029 顆
   神經元，但 0 條連線；八套的判準是 `dense ∧ is_data_source`，而 FANC 沒有 `dense`。
2. **`VFB_00050000`（L1 幼蟲 ssTEM）的 `Domains` 是 0**——要確認是真的沒有畫分區，
   還是我們的算法漏了。表上那個 0 目前只有一條路驗過。
3. **`reports/<編號>` 現在 302 指向 v2**（第 17 條）。這等於把學員固定在舊版介面上，
   而 v3 有六處欄位名不同。要嘛接受並在每頁註明（現在的做法），
   要嘛改推 v3 的網址格式——**這是個還沒做的決定，不是一個 bug。**
4. ~~第七道稽核還沒寫~~ ——**已完成**（第 47 條，`scripts/field_audit.py`）。
   剩下的天花板是**跨版本的張冠李戴**：v2 叫 `License`、v3 叫 `Licenses`，
   兩個都在探針輸出裡，所以把 v2 那一欄寫成 `Licenses` 它抓不到。
   要補的話，探針得記下「這個字串是在哪一個網站上拍到的」，稽核再按網站比對。
5. **`NeuronsSynaptic` 的 464 還原不出來**（第 40 條，最好只到 250）。
   PART 4 算連線數會遇到同一類問題，到時候要嘛還原成功、要嘛照這次的界線寫。
6. **REST 報 60,002、知識庫算出 59,739，差 263 列查不出來**（第 62 條）。
   機制已經確認（每個「（個體, 類別）」配對一列），也排除了模板那個可能，
   **但餘數沒有解釋**。已寫在 `out/recompute/cross_check.json` 的 `note` 與 PART 8 第 5 節。
7. **PART 8 對不上的那三類，只剩一處**（PART 8 第 7 節）：
   ~~A 類 18 個找不到~~、~~7 列不知為何沒收~~、~~`R7p` 的 200 顆~~
   ——**全部已解**（第 72–74 條）：**兩邊差了一個版本**，VFB 是 `v1.0.1`、
   論文表是 `v1.1`，改動全寫在論文的〈Versions of the dataset〉裡。
   剩下的兩句是**方法的天花板**，不是待辦：
   （a）沒有 v1.1 的存取權，所以「那 11 顆是 v1.1 併出來的」是推論不是查證；
   （b）論文說的「2 個型併成 1 個」用代表編號驗不出來（第 75 條）。
   **仍在的只有一條**：B 類扣掉改名後還有 **172** 個名字，**只抽查了 3 個**。
8. ~~`run_meta.json` 與 `paper_quotes.json` 沒有程式產生~~ ——**已解**（第 88、89 條）：
   新增 `recompute/run_meta.py`（含乾淨重跑）與 `recompute.py` 的 `paper_quotes()`。
   **孤兒檔現在由程式報**（`clean_run.not_regenerated`），不再靠人想起來。
9. **第八道只成了一半**（第 48 條）：`refs`（指涉詞）已併進 `content_audit.py`；
   「未綁定的簡稱」兩種設計都失敗、已移除，**不要再做一次**（原因見第 48 條）。
   以下是原本的構想，留作紀錄——
   規範第 10 節第 5 道與第 6 節那兩條目前**完全沒有程式在守**，
   而第 45 條證明人工複查抓不到——**造簡稱的人不會發現簡稱沒綁定**。
   抽法不難，兩道各抽一次、印出前後文給人判斷：
   - **指涉詞**：`那\s*[一二三四五六七八九十百]+\s*[支個張條節]`、`上面那張`、
     `下圖`、`上一節`、`前面那`、`這張表`——列出每一處與前後 60 字，
     問一次「它指的東西，在這一句裡叫得出名字嗎」。
   - **未綁定的簡稱**：抽出正文裡用 `<b>` 標起來、但**沒有出現在任何 `<table>` 或
     `<code>` 裡**的中文短詞，看它第一次出現的位置有沒有跟一個具名對象綁在一起。
   第二道會有誤判（很多 `<b>` 只是強調），所以定位是**抽出來給人看**，
   不是自動判對錯——跟 `content_audit.py` 現有的三道同一個性質，可以直接加進去。

**另外有六條不是待辦，是往後每一頁都要照做的規矩**（寫在這裡以免被當成未完成）：

- 凡是「本課以 X 為準」這類裁決句，裁決要寫在爭議旁邊（第 14 條）。
- 作業單每一步要答三件事：**哪一個網域、去哪裡看、看到什麼算對**（第 19 條）。
- 作業單裡「點下去會看到某結果」的句子，**要有一支探針真的點下去**（第 44 條）。
- 寫介面描述時 **v2 與 v3 兩版都要查**，六處欄位名不同（第 31 條）。
- 服務端的總計數字要標日期，不要當常數（`types/tool.md`）。
- **寫 `_notes` 時把頁面上每一句斷言的依據唸一遍**，特別是從觀察跳到通則的句子
  （第 36、43 條，同一型犯過兩次）。
